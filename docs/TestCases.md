# Q-DetectVision 测试用例文档

## 文档信息

| 属性 | 值 |
|------|------|
| 文档版本 | v1.0 |
| 创建日期 | 2026-05-19 |
| 适用版本 | Q-DetectVision v1.0 |

---

## 目录

1. [认证服务测试用例](#1-认证服务测试用例)
2. [方案管理测试用例](#2-方案管理测试用例)
3. [视觉工具测试用例](#3-视觉工具测试用例)
4. [数据库测试用例](#4-数据库测试用例)
5. [工具链执行测试用例](#5-工具链执行测试用例)
6. [集成测试用例](#6-集成测试用例)
7. [系统测试用例](#7-系统测试用例)

---

## 1. 认证服务测试用例

### 1.1 登录功能

| 用例ID | TC-AUTH-001 |
|--------|-------------|
| 测试目标 | 验证有效凭证登录成功 |
| 前置条件 | AuthService已初始化 |
| 测试步骤 | 1. 调用login("admin", "admin123") |
| 预期结果 | 返回true，isAuthenticated()返回true，currentUser()返回"admin" |
| 优先级 | P0 |

| 用例ID | TC-AUTH-002 |
|--------|-------------|
| 测试目标 | 验证无效用户名登录失败 |
| 前置条件 | AuthService已初始化 |
| 测试步骤 | 1. 调用login("nonexistent", "password") |
| 预期结果 | 返回false，isAuthenticated()返回false |
| 优先级 | P1 |

| 用例ID | TC-AUTH-003 |
|--------|-------------|
| 测试目标 | 验证错误密码登录失败 |
| 前置条件 | AuthService已初始化 |
| 测试步骤 | 1. 调用login("admin", "wrongpassword") |
| 预期结果 | 返回false，isAuthenticated()返回false |
| 优先级 | P1 |

| 用例ID | TC-AUTH-004 |
|--------|-------------|
| 测试目标 | 验证空凭证登录失败 |
| 前置条件 | AuthService已初始化 |
| 测试步骤 | 1. 调用login("", "") |
| 预期结果 | 返回false |
| 优先级 | P1 |

| 用例ID | TC-AUTH-005 |
|--------|-------------|
| 测试目标 | 验证登出功能 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 登录成功后调用logout() |
| 预期结果 | isAuthenticated()返回false，currentUser()为空 |
| 优先级 | P0 |

### 1.2 密码修改功能

| 用例ID | TC-AUTH-006 |
|--------|-------------|
| 测试目标 | 验证正确旧密码修改成功 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 调用changePassword("admin", "admin123", "newpass") |
| 预期结果 | 返回true，使用新密码可登录 |
| 优先级 | P1 |

| 用例ID | TC-AUTH-007 |
|--------|-------------|
| 测试目标 | 验证错误旧密码修改失败 |
| 前置条件 | 用户已登录 |
| 测试步骤 | 1. 调用changePassword("admin", "wrong", "newpass") |
| 预期结果 | 返回false |
| 优先级 | P1 |

---

## 2. 方案管理测试用例

### 2.1 方案创建与序列化

| 用例ID | TC-SCHEME-001 |
|--------|---------------|
| 测试目标 | 验证方案创建 |
| 前置条件 | SchemeManager已初始化 |
| 测试步骤 | 1. 创建Scheme对象 |
| 预期结果 | 对象创建成功，ID非空 |
| 优先级 | P0 |

| 用例ID | TC-SCHEME-002 |
|--------|---------------|
| 测试目标 | 验证方案序列化/反序列化 |
| 前置条件 | Scheme对象已创建 |
| 测试步骤 | 1. 调用serialize() 2. 调用deserialize() |
| 预期结果 | 反序列化后内容与原对象一致 |
| 优先级 | P0 |

| 用例ID | TC-SCHEME-003 |
|--------|---------------|
| 测试目标 | 验证JSON验证（缺少必填字段） |
| 前置条件 | Scheme对象已创建 |
| 测试步骤 | 1. 构造缺少"name"字段的JSON 2. 调用deserialize() |
| 预期结果 | 返回false |
| 优先级 | P1 |

| 用例ID | TC-SCHEME-004 |
|--------|---------------|
| 测试目标 | 验证JSON验证（类型错误） |
| 前置条件 | Scheme对象已创建 |
| 测试步骤 | 1. 构造"name"为数字类型的JSON 2. 调用deserialize() |
| 预期结果 | 返回false |
| 优先级 | P1 |

### 2.2 SchemeManager单例

| 用例ID | TC-SCHEME-005 |
|--------|---------------|
| 测试目标 | 验证单例模式 |
| 前置条件 | 无 |
| 测试步骤 | 1. 两次调用instance() |
| 预期结果 | 返回同一实例 |
| 优先级 | P1 |

---

## 3. 视觉工具测试用例

### 3.1 TemplateMatchTool

| 用例ID | TC-VISION-001 |
|--------|---------------|
| 测试目标 | 验证有效参数配置 |
| 前置条件 | TemplateMatchTool已创建 |
| 测试步骤 | 1. 传入threshold=0.8, method="CCOEFF_NORMED" |
| 预期结果 | configure()返回true |
| 优先级 | P0 |

| 用例ID | TC-VISION-002 |
|--------|---------------|
| 测试目标 | 验证无效阈值配置 |
| 前置条件 | TemplateMatchTool已创建 |
| 测试步骤 | 1. 传入threshold=1.5 |
| 预期结果 | configure()返回false |
| 优先级 | P1 |

| 用例ID | TC-VISION-003 |
|--------|---------------|
| 测试目标 | 验证模板路径验证 |
| 前置条件 | TemplateMatchTool已创建 |
| 测试步骤 | 1. 调用validateTemplatePath()传入无效路径 |
| 预期结果 | 返回false |
| 优先级 | P1 |

### 3.2 EdgeDetectTool

| 用例ID | TC-VISION-004 |
|--------|---------------|
| 测试目标 | 验证阈值顺序验证 |
| 前置条件 | EdgeDetectTool已创建 |
| 测试步骤 | 1. 传入lowThreshold=150, highThreshold=50 |
| 预期结果 | configure()返回false |
| 优先级 | P1 |

### 3.3 ToolFactory

| 用例ID | TC-VISION-005 |
|--------|---------------|
| 测试目标 | 验证创建已知工具类型 |
| 前置条件 | ToolFactory已初始化 |
| 测试步骤 | 1. 调用createTool("TemplateMatch") |
| 预期结果 | 返回非空指针，type()返回"TemplateMatch" |
| 优先级 | P0 |

| 用例ID | TC-VISION-006 |
|--------|---------------|
| 测试目标 | 验证创建未知工具类型 |
| 前置条件 | ToolFactory已初始化 |
| 测试步骤 | 1. 调用createTool("UnknownTool") |
| 预期结果 | 返回nullptr |
| 优先级 | P1 |

---

## 4. 数据库测试用例

### 4.1 结果插入与查询

| 用例ID | TC-DB-001 |
|--------|-----------|
| 测试目标 | 验证单条结果插入 |
| 前置条件 | 数据库已打开 |
| 测试步骤 | 1. 调用insertResult() |
| 预期结果 | 返回true，记录数为1 |
| 优先级 | P0 |

| 用例ID | TC-DB-002 |
|--------|-----------|
| 测试目标 | 验证按方案ID查询 |
| 前置条件 | 已插入多条记录 |
| 测试步骤 | 1. 调用queryResults("scheme-001") |
| 预期结果 | 返回该方案的所有记录 |
| 优先级 | P0 |

### 4.2 安全删除保护

| 用例ID | TC-DB-003 |
|--------|-----------|
| 测试目标 | 验证大量数据删除保护 |
| 前置条件 | 数据库中已有>10条记录 |
| 测试步骤 | 1. 调用deleteResults("", true) |
| 预期结果 | 返回false，记录未删除 |
| 优先级 | P0 |

| 用例ID | TC-DB-004 |
|--------|-----------|
| 测试目标 | 验证单方案删除 |
| 前置条件 | 数据库中已有多方案记录 |
| 测试步骤 | 1. 调用deleteResults("scheme-001", false) |
| 预期结果 | 返回true，仅删除该方案记录 |
| 优先级 | P1 |

---

## 5. 工具链执行测试用例

| 用例ID | TC-TOOLCHAIN-001 |
|--------|------------------|
| 测试目标 | 验证空工具链执行 |
| 前置条件 | ToolChainExecutor已创建 |
| 测试步骤 | 1. 调用execute(input) |
| 预期结果 | 返回true |
| 优先级 | P1 |

| 用例ID | TC-TOOLCHAIN-002 |
|--------|------------------|
| 测试目标 | 验证多工具执行 |
| 前置条件 | ToolChainExecutor已配置多个工具 |
| 测试步骤 | 1. 添加EdgeDetect和TemplateMatch工具 2. 调用execute() |
| 预期结果 | 返回true，工具链正常执行 |
| 优先级 | P0 |

---

## 6. 集成测试用例

| 用例ID | TC-INT-001 |
|--------|------------|
| 测试目标 | 验证方案保存与数据库集成 |
| 前置条件 | 认证通过 |
| 测试步骤 | 1. 创建方案 2. 保存到SchemeManager 3. 插入检测结果 |
| 预期结果 | 方案保存成功，结果可查询 |
| 优先级 | P0 |

| 用例ID | TC-INT-002 |
|--------|------------|
| 测试目标 | 验证认证与方案管理集成 |
| 前置条件 | 无 |
| 测试步骤 | 1. 登录 2. 访问SchemeManager 3. 登出 |
| 预期结果 | 全程无异常 |
| 优先级 | P0 |

---

## 7. 系统测试用例

| 用例ID | TC-SYS-001 |
|--------|------------|
| 测试目标 | 验证完整检测工作流 |
| 前置条件 | 系统已启动 |
| 测试步骤 | 1. 登录 2. 创建方案 3. 配置工具链 4. 执行检测 5. 保存结果 6. 查询结果 7. 登出 |
| 预期结果 | 所有步骤成功执行 |
| 优先级 | P0 |

| 用例ID | TC-SYS-002 |
|--------|------------|
| 测试目标 | 验证多方案管理 |
| 前置条件 | 系统已启动 |
| 测试步骤 | 1. 创建多个方案 2. 分别插入结果 3. 按方案查询 |
| 预期结果 | 各方案数据隔离正确 |
| 优先级 | P1 |

| 用例ID | TC-SYS-003 |
|--------|------------|
| 测试目标 | 验证会话生命周期 |
| 前置条件 | 系统已启动 |
| 测试步骤 | 1. 未登录状态检查 2. 登录 3. 执行操作 4. 登出 5. 验证未登录状态 |
| 预期结果 | 会话状态正确切换 |
| 优先级 | P0 |

---

## 附录：测试用例统计

| 模块 | 用例数 | P0 | P1 |
|------|--------|----|----|
| 认证服务 | 7 | 2 | 5 |
| 方案管理 | 5 | 2 | 3 |
| 视觉工具 | 6 | 2 | 4 |
| 数据库 | 4 | 2 | 2 |
| 工具链 | 2 | 1 | 1 |
| 集成测试 | 2 | 2 | 0 |
| 系统测试 | 3 | 3 | 0 |
| **总计** | **29** | **14** | **15** |