# 自我升级模型 0903：大模型 × 工业检测深度模型的「便捷性」与「在线自学习」多智能体对话录

> 生成日期：2026-09-03　|　版本：**v2（三方深化版）**
>
> **v2 修订说明（09-03 下午）**：第一部分为甲 / 乙双人对话，用于收敛总体框架；第二部分起角色扩展并重定位为三人 —— **甲 = 软件开发工程师**（系统架构 / 代码实现 / 工程规范 / 交付可行性）、**乙 = 算法编写工程师**（算法设计 / 模型与策略选型 / 精度与性能优化）、**丙 = 现场运维工程师**（部署实施 / 运行监控 / 故障排查 / 现场环境与资源约束）。第一部分的"甲"可视为甲的架构视角，其结论在第二部分继续有效，但须接受三方复核。
>
> 讨论范围：**仅方案探讨**。不编译、不修改 `E:\anchor\Trae\QDV` 任何现有代码。

---

## 目录

- **第一部分：双人对话 —— 框架收敛（甲 / 乙，10 轮）** …… §一 ~ §四
- **第二部分：三方对话 —— 落地深化（甲 / 乙 / 丙，10 轮）** …… §五 ~ §七
  - 第 11 轮 云端大模型能不能进车间：算力、网络与实时性的重新分层
  - 第 12 轮 难例洪水：夜班八小时谁来复核
  - 第 13 轮 特征库存哪、多大、怎么同步
  - 第 14 轮 检测增量学习的背景类陷阱
  - 第 15 轮 开放集与"没见过"：三态输出的杀手锏
  - 第 16 轮 指标可比性：把复检数据变成免费的持续评估集
  - 第 17 轮 四维体检与故障树：一键定位是模型坏了还是灯坏了
  - 第 18 轮 换型：一天四次的配方切换
  - 第 19 轮 代码架构：单向数据流、独立进程与一键降级
  - 第 20 轮 工期、成本与优先级重排

---

---

> 参考项目：`E:\anchor\Trae\QDV`（QDV 主程序 + `tools\qdv-annotator` 标训闭环 + `training\yolo_train.py` + 单丝计数业务）
> 参考依据：`docs\AI模块设计文档.md`、`docs\算子库改进报告.md`、`软硬件基本配置.md`、`tools\qdv-annotator\QDVAnnotator_闭环整合设计方案_2026-08-19.md`

---

## 一、智能体的设定（第一场：甲 / 乙）

| | **甲 —— 软件开发工程师** | **乙 —— 算法编写工程师** |
|---|---|---|
| 代号 | 锚（Anchor） | 流（Flow） |
| 立场 | 系统架构 / 代码实现 / 工程规范 / 交付可行性（第一场中以架构视角发言） | 算法设计 / 模型与策略选型 / 精度与性能优化 |
| 关注序 | 节拍 > 可追溯 > 稳定 > 精度 > 新颖 | 精度上限 > 数据效率 > 自动化 > 部署 |
| 口头禅 | 「谁来签字？出问题谁背？」 | 「但这样精度天花板就被锁死了。」 |
| 代表性坚持 | 产线主模型永不在线反向传播 | 模型必须能从真实流水中持续变强 |
| 思维习惯 | 先算时间与钱：一张图几毫秒、一个误判多少钱 | 先算数据与分布：这个样本见过没有、梯度往哪走 |

**共同约束（双方都接受，作为讨论红线）**

1. 产线节拍：20 个工位，单帧约 100 ms，其中 6 个工位计划升级为视频/时序检测。
2. 硬件：RTX 5060 Laptop 8 GB 显存、CUDA 12.8；训练侧存在页面文件不足导致被系统强杀的历史事故。
3. QDV 现状：`InferenceEngine`（OpenCV DNN / ONNX Runtime 双后端）、`ModelManager`（manifest.json + SHA256 + LRU + `.trash` 事务删）、`TrainingBridge`（QProcess 拉 Python + 心跳检测）、`DatasetValidator`（四维校验 / 健康分 100−30C−10E−2W）、QDVAnnotator 的「标注 → yolo_obb 导出 → 训练 → 权重回灌 8765」人工闭环（T6 一键回灌尚未完成）。
4. 业务实测数（单丝计数 V2.0）：val 52 张、GT=2358，conf=0.10 时 recall=94.444%、漏检=5.556%、**过检=41.009%**；conf 提到 0.30 后端点数从 1930 降到 1737。高密度（27–60 根）与低密度（6–24 根，tips-up）两场景**必须分开评估**。

---

## 二、对话正文

### 第 1 轮 —— 开场：命题交锋

**乙**：我先抛结论：工业检测里深度学习模型「不好用」，根子不在精度不够，而在**它不会自己变好**。我们交付一个 YOLOv8n-obb 过去，第一周 92 分，三个月后 78 分，因为光源老化、来料批次换了、镜头进灰。然后就是工程师出差、重新标注、重新训练。这套成本才是行业真正的痛点。所以我的主张很直接——**让小模型在检测过程中，拿实时捕获的图样做在线自学习，越跑越准**。这事儿以前做不了，是因为没有便宜的"裁判"；现在大模型能当裁判，闭环就成立了。

**甲**：你这话我同意前半段，不同意后半段。前半段——三个月后掉到 78 分——这不是假设，这就是我们现场。但"边跑边学"这四个字在我这儿是要挨骂的。我问你三件事：第一，20 个工位、单帧 100 ms，你打算在哪个 100 ms 里插反向传播？第二，模型昨天判 OK 今天判 NG，同一颗料，客户审核怎么解释？第三，训练抢显存，8 GB 卡上训 batch=8 的时候推理直接掉帧甚至 OOM，停机一分钟产线要赔多少钱？

**乙**：这三个问题都有解，但我不急着反驳，因为它们问的是"怎么学"，不是"该不该学"。

**甲**：那我们就先把它问清楚：**你到底要"模型变聪明"，还是要"工程师少出差"？** 这俩不是一回事。

---

### 第 2 轮 —— 重新定义「便捷性」

**甲**：我说便捷性。你现在去产线看，一个调试工程师配一套 QDV 方案，最花时间的不是训练，是**配**。选哪个算子、ROI 画哪儿、阈值给多少、Blob 的面积上下限怎么定、光照变了要不要加预处理。我们 `operators.json` 里 70 个算子，新人看到就懵。大模型最该干的活是**把这个"配"的门砍掉**——工程师说一句"我要检电芯蓝膜棱边的压痕，宽度超过 0.2 mm 算不良"，系统自动给他搭出一条算子链、给出初值、还告诉他为什么这么配。这是**当天就能省钱**的事。你那个在线自学习，最快也要三个月才见效。

**乙**：便捷性我认同要砍"配"的门，但你说"自学习三个月才见效"是把它的价值算小了。我给你算另一笔账：单丝计数现在的过检率 **41%**。意思是每报 100 次 NG，41 次是假的。下游复检工位的人按报警数配的，这 41% 全是人力成本，天天在烧。它不是一次性成本，是**永续成本**。而在线自学习真正解决的不是"三个月后掉分"，是**这 41% 能随着产线运行自己往下掉**——因为每来一个新花样，模型就记住一次。

**甲**：41% 这个数确实扎心。但你有没有想过，它掉不下来的原因，未必是模型不行？conf=0.10 时端点数 1930，conf=0.30 时 1737，GT 是 2358。这说明什么？说明**阈值本身就是错的**——我们一直在漏，同时也在过。这种问题，靠模型在线学习一年都未必修得好，靠一次正经的阈值标定 + 分场景校准，可能一下午就修好了。

**乙**：你这句话我接，而且它其实点到了要害：**你把"学习"定义成"改权重"，所以觉得它慢又危险。但参数校准本身就是一种学习。** 阈值标定如果让系统自己根据历史 score 分布去定，而不是工程师拍脑袋，它就是在线自学习的最轻形态。

**甲**：……你这是把我的例子抢去当你的论据。

**乙**：不是抢，是它本来就是同一件事的两端。我要的"在线自学习"是一个**谱系**，不是一个动作。谱系的一端是"调个数"，另一端是"改权重"。你怕的是改权重那一端，我承认那一端确实该怕。但我们能不能先把谱系画出来，再决定每一端放在什么位置？

**甲**：这个我愿意谈。**你把谱系画出来，我给你标哪里能上产线、哪里只能待在实验室。**

---

### 第 3 轮 —— 大模型站哪一层？

**乙**：那先定大模型的位置。我给三层：**前台翻译层、中台裁判层、后台教练层**。

- **前台翻译层**：LLM 把自然语言需求翻译成 QDV 的算子链 / 参数初值 / `dataset.yaml`。解决你说的"配"的门。
- **中台裁判层**：VLM 对模型"拿不准"的样本出判定意见和伪标签。这是在线自学习的**燃料来源**——没有裁判就没有标签，没有标签就谈不上学习。
- **后台教练层**：LLM 编排训练：难例怎么采样、类别怎么平衡、超参怎么给、训练崩了看日志归因。

**甲**：前两层我认，第三层我觉得是锦上添花。但你漏了一层更要命的——**诊断层**。现场设备出问题，工程师第一反应是"模型坏了"，其实八成是光源、来料、机械定位变了。LLM 看日志、看 score 曲线、看近期图缩略，直接说"你的同轴光比上周暗了 12%，先查电源"，这比"帮你调阈值"有用十倍。

**乙**：这个我加进去，但它有个前提：**LLM 的意见必须能被证伪**。你说"光源暗了 12%"，得给出一个可测量的证据链——比如过去 7 天 ROI 内灰度均值的滑动曲线，以及它和 score 分布漂移的相关系数。否则就是幻觉，工程师信一次就不再用了。

**甲**：对，**这是我对大模型进工业最核心的一条要求：它可以猜，但必须给证据；它的输出必须落到可校验的结构化对象上，而不是一段话。** 你说"翻译成算子链"，那就输出 JSON，我们的 `ToolChainVerifier` 去验算子存在性、端口类型兼容性、参数范围；验不过就不让进画布。这正好接上 P1-2 那个「元数据—实现一致性校验」——大模型最会干的事就是瞎编一个不存在的算子，比如它会给你一个 `Reconstruct3D`，而我们那玩意儿**压根没实现**。

**乙**：（笑）这个我信，它连不存在的论文都编得出来，何况算子名。

**甲**：所以前台那层，我的规矩是：**LLM 出草稿，静态校验 + 沙盒试跑 + 人确认，三步缺一不可。** 而且试跑必须用历史图集，不能拿正在跑的产线当试验田。

**乙**：同意。那裁判层呢？你的规矩是什么？

**甲**：裁判层的规矩更简单粗暴：**VLM 永远不能直接决定 OK/NG 进判定链。** 它可以当"第二意见"，可以预标注，可以降低人工复核量，但它输出的东西只要进过一次产线判定，追溯体系就废了——你没法向客户解释这个结论是哪个版本的哪个模型、在什么温度下算出来的。

**乙**：这条我不反对，但我加一句：**VLM 的伪标签要经过"置信度分层"**。高置信的直接进自动训练集，低置信的推给人工，中间的进"待观察池"累积证据。

**甲**：这个可以。但我提醒你一件事——我们踩过一次坑：全自动导出数据集的时候，一次误操作生成了 **177 个 0 字节空标签**，训出来 mAP=0，整个数据集废掉。教训是：**自动化链条上每一环都要有"非空门禁"**。你的伪标签管线，我要求出口必须有和 `DatasetValidator` 同级的体检——配对率、非空标签率、类别分布，任一为 0 直接中止，不许往下走。

**乙**：这条我完全接受，而且应该写进红线：**没有门禁的自动化，比不做自动化更危险。**

---

### 第 4 轮 —— 正面碰撞：在线自学习到底怎么学？

**乙**：好，进入正题。我的方案叫 **Shadow-Learn（影子学习）四段式**：

1. **难例捕获**：主模型正常推理，但对每张图算一个"不确定度"（检测任务用框 score 熵 + 框数偏离历史均值程度；分类用 top-1/top-2 margin）。低于阈值的，图 + 上下文（工位号、批次、光照参数、上游算子输出）进难例队列。
2. **裁判标注**：难例队列先过 VLM 预标注，高置信自动吸收，低置信推人工复核。人工复核在 QDVAnnotator 里做，界面已经现成。
3. **影子训练**：用难例 + 历史回放集，训一个候选模型。训练在**独立进程 / 独立显存配额**里跑，绝不动主模型。
4. **门控回灌**：候选模型先在冻结的 golden set 上跑回归，指标达标才灰度上线，先 5% 流量，再 50%，再全量。

**甲**：四个问题，一个一个来。
**其一，不确定度怎么定阈值？** 你设了阈值，就意味着有一部分"模型其实错了但很有自信"的样本被你放过去了。过检 41% 里，很可能一大半是**高置信的错**——模型非常确定那里有个端点，其实没有。你的捕获器抓不到它们。

**乙**：这是硬伤，我认。补法有两个：一是**不只看不确定度，还看"结果分布漂移"**——某个工位的判定 NG 率从 3% 跳到 9%，哪怕每张图都很自信，也要触发抽检；二是**用下游信号做监督**——复检工位的最终结果、后工序的良率、甚至客户退货数据，是最硬的标签源，虽然延迟高。

**甲**：其二，**训练放哪儿跑？** 你说独立显存配额。8 GB 卡，主模型推理占 2 GB，你训个 YOLOv8n 用 batch=8 大概要 4–5 GB，剩 1 GB 给系统，页面文件一抖就 OOM。而且我们这边有真实事故：页面文件不足，训练被系统强杀，整个 GUI 闪退。**你打算让产线工控机去赌这个？**

**乙**：不赌。所以"在线"这个词我要重新定义——**生产在线读数，时间准在线更新**。采集、裁判、难例入库，这些是流式的、跟着产线走；但真正的**权重更新发生在班后、换型间隙、或者夜班空闲窗口**。第二天开班生效。这在工程上是"天级在线"，在算法上是"增量学习"。

**甲**：这句我听进去了。天级更新我能签字，毫秒级更新我签不了。
**其三，灾难性遗忘。** 你拿难例去微调，难例天然是长尾分布，训三轮下来，模型把常见样本忘了，头部类别掉点。你怎么防？

**乙**：三个手段叠起来：
- **冻结骨干，只训检测头**（或者 LoRA 低秩适配），能更新的参数量控制在 5% 以内；
- **EWC（弹性权重固化）**：用 Fisher 信息矩阵估出"哪些参数对旧任务重要"，重要的参数加二次惩罚；
- **Replay buffer**：每次训练强制混入 golden set 的采样，比例不低于 30%。

**甲**：其四，也是我最担心的——**你怎么证明新模型比旧模型好？** golden set 是冻结的历史集，你在它上面刷分，刷到最后就是过拟合到 golden set。而且单丝那个项目，高密度场景 27–60 根、低密度 6–24 根 tips-up，**两个场景的分布差得远**，一个模型在两个场景上混训，指标一定互相拉扯。

**乙**：所以 golden set 必须**分层冻结**：按工位、按场景、按批次、按缺陷类型分层，每层独立出指标，任一层回退超过阈值就否决。这跟你们 `DatasetValidator` 的"分布维度"校验是一个思路，只是把它从事前挪到了事后，从"能不能训"变成"能不能上"。

**甲**：行，你这条算把我的问题接住了。但说实话，**我觉得你的方案整体重了一档**。我来给个我能落地的版本。

---

### 第 5 轮 —— 甲的反提案：把「学习」拆成三层

**甲**：我要把"学习"拆成三层，只有一层涉及权重。

| 层 | 学什么 | 更什么 | 生效时延 | 可回滚性 | 我的态度 |
|---|---|---|---|---|---|
| **L0 参数自适应** | 阈值、ROI、归一化统计量、曝光补偿 | 一组标量 / 查表 | 秒级热更 | 极好（存快照即可） | **立刻可做** |
| **L1 原型记忆** | 样本 embedding 与历史判定的对应关系 | 特征库（bank） | 毫秒级（写入即生效） | 极好（删一条原型即刻失效） | **最被低估** |
| **L2 权重更新** | 模型参数 | ONNX 权重文件 | 天级（班后训、次日生效） | 中（靠版本链 + SHA256） | **影子+门控后才做** |

**L0 是什么？** 就是把我刚才说的"阈值拍脑袋"自动化。具体做法：维护每张图推理 score 的滑动分布，把 conf 阈值锚定到历史分布的**分位数**上，而不是写死 0.25。单丝那个例子，conf 从 0.10 到 0.30 端点数掉 200 个，说明阈值极度敏感——那就别让它敏感，让它**跟着分布漂**。同理，光照漂移用 ROI 内灰度均值的 EMA 做归一化补偿。这层全是数字，改错了恢复只要一秒。

**乙**：L0 我完全同意，但我要指出它的**致命诱惑**：阈值自适应会让指标看起来很漂亮，同时**掩盖真实的能力退化**。模型本来该报警说"我看不清了"，结果你调调阈值把它盖过去了。

**甲**：对，所以 L0 必须配一条铁律：**阈值只能朝"更严"的方向自动收紧，朝"更松"的方向必须人工确认。** 而且任何一次 L0 调整都要写进审计日志，附上触发时的漂移证据。

**乙**：这条铁律我加一句：**L0 的调整幅度要有上下限钳位**，比如 conf 阈值只能在基线 ±30% 内浮动。超出说明不是漂移，是换型或者故障，该触发的是报警不是自适应。

**甲**：好，成交。那我说 L1——**这是我认为整个讨论里最值钱的部分，但我们俩之前都没提。**

**L1 原型记忆**的核心是：**不改权重，改记忆。** 骨干网络冻结，把每张图的特征向量存进一个特征库，附带它的最终判定（人工确认过的）。新图来了先查库：

- **命中高相似原型**（余弦 > 0.95）→ 直接复用历史判定，连检测头都不用跑，**又快又稳**；
- **相似度中等**（0.85–0.95）→ 走正常推理，但把结果和原型判定做交叉验证，不一致就进难例队列；
- **相似度低**（< 0.85）→ 这就是**全新的花样**，必定进难例队列。

**乙**：……你这个设计，本质上是**参数无关的最近邻持续学习**。它确实有三个权重更新给不了的好处：一是**零遗忘**，因为根本没有梯度；二是**可解释**，"我判 OK 是因为它跟 3 号原型 0.97 相似"，这句话审核员听得懂；三是**可撤销**，某条原型后来发现是错的，删掉立刻生效，不用重训。

**甲**：而且它还有个你现在最需要的东西：**冷启动**。新工位上线第一天，一张标注都没有。怎么办？你 L2 的增量训练没数据可训。但 L1 可以——**先用零样本模型跑**（QDV 里已经集成了 ZeroShotKit，`models/` 下面躺着 `clip`、`grounding_sam`、`zero_shot` 三个目录呢），第一天就是零样本冷启动，人工复核的结果直接沉淀成原型库；跑一个月，原型库攒起来了，再用原型库去蒸馏一个小模型。这时候小模型的训练数据是**高质量的、真实的、带上下文的**。

**乙**：你把我的 L2 变成了 L1 的下游产物。这个顺序是对的——**先有记忆，再有模型**，而不是反过来。

**甲**：对。工业场景和互联网场景最大的区别就是：**工业没有海量数据，只有海量重复**。L1 就是为"海量重复"设计的——90% 的图是见过的，那就别每张都重新算一遍。

**乙**：但 L1 有个规模问题。特征库无脑增长，三个月后几十万条，检索变慢，而且里面 90% 是冗余的正常样本。

**甲**：所以要做 **core-set 选择**：只保留"边界样本"和"代表性样本"。正常的、高度相似的样本聚成一簇，簇心留一条；只有落在簇边缘的、或者被判定为异常的，才单独留。经验上**控制在 2000 条以内**，用 FAISS 之类的索引，百万级检索是亚毫秒。

**乙**：这条我加个算法细节：用 **herding / k-center greedy** 做 core-set 选择，保证特征空间覆盖度而不是只保数量。另外，原型库本身也可以按"工位—批次—缺陷类型"分桶，检索时只查同桶，既快又避免跨场景污染——正好解决你前面说的"高低密度不能混"。

**甲**：对，**分桶是必须的**。高密度 27–60 根和低密度 6–24 根，物理上就不是一回事，混在一起检索就是灾难。

---

### 第 6 轮 —— 标签从哪来：裁判层的经济学

**乙**：L1 和 L2 都解决了，现在回到最原始的问题：**标签从哪来？** 这是我整个方案的燃料。我给四个来源，按单价和延迟排：

| 来源 | 单价 | 延迟 | 质量 | 适用 |
|---|---|---|---|---|
| **下游工序反馈**（复检结果 / 后工序良率 / 客户退货） | 几乎 0（已有数据） | 小时~天级 | 高但稀疏、有偏 | 做**趋势监督**，不做逐样本监督 |
| **VLM 伪标签** | 约 0.1–1 元/图 | 秒级 | 中（依赖 prompt 和任务难度） | 难例**预标注**，降低人工量 |
| **人工复核** | 数十元/小时 | 分钟级 | 高（金牌标准） | 低置信难例 + 抽检校验 VLM |
| **合成缺陷** | 极低（算力） | 分钟级 | 未知（有 domain gap） | 稀缺类别的**冷启动平衡** |

**甲**：我先砍掉一个：**合成缺陷别进主训练集。** 我知道现在 diffusion 造缺陷很火，但工业缺陷的"真"在于它的**成像链路**——光源角度、材质反光、镜头畸变、sensor 噪声。你生成的缺陷图像，模型学到的是"生成器的先验"，不是"相机的物理"。我们做蓝膜棱边压痕，那个压痕在不同角度的明暗过渡是有物理规律的，GAN 造出来的东西，人看着像，模型学着歪。

**乙**：我不反对"别进主训练集"，但我要给合成数据一个**更准确的位置**：它不用来教模型"缺陷长什么样"，它用来教模型"**什么不是缺陷**"。也就是做**负样本的多样性增强**——真实的正常样本往往很单一（就那几种光照、那几种姿态），合成可以造出无穷无尽的"正常但不同"，让模型的决策边界收紧。过检 41% 里，很大一部分就是决策边界太松，正常样本的多样性没覆盖到。

**甲**：……这个用法我可以接受。收紧边界而不是教特征，domain gap 的伤害小得多。但验证方式要变：**合成数据只能在 golden set 上证明"过检率下降"，不能拿它自己当验证集。** 自己造的数据自己验，等于没验。

**乙**：同意。那 VLM 伪标签呢？你的顾虑是什么？

**甲**：三个。第一，**VLM 不知道你们的判定标准**。你们的 OK/NG 边界是技术协议里写的，0.2 mm 压痕算不良、0.15 mm 不算，VLM 没有这把尺子，它凭"看起来像不像"判，跟标准不是一回事。
第二，**它对小目标不敏感**。单丝端点在 3072×2048 图上可能就十几个像素，你整图丢给 VLM，它看得见才怪。
第三，**它不稳定**。同一个 prompt 今天判 OK 明天判 NG，你没法追溯。

**乙**：三个都有解，但都得靠工程手段，不是靠换更强的模型：
- **尺子问题** → 把判定标准写进 prompt，并且**给参照物**。图里放一个已知尺寸的标准块/标尺，或者 cropping 时固定物理视野（已知 pixel scale，QDV 的 `UnitConvert` 算子做的不就是这个？），prompt 里直接说"这块区域实际尺寸是 3 mm × 2 mm，压痕宽度超过 0.2 mm 判 NG"。
- **小目标问题** → **不要让 VLM 从头找**。让小模型先出候选框，VLM 只做**裁剪后的二分类/三分类裁判**（确认 / 否决 / 拿不准）。这也把 VLM 的输入从全图降到 224×224 的小 crop，成本和延迟都降一个量级。
- **不稳定问题** → **同一条难例让 VLM 判三次，temperature=0 + 固定 seed，取多数投票；三次不一致的，直接降级为人工复核**。一致性本身就是置信度信号。

**甲**："小模型出候选、VLM 做裁判"——这个分工我认，因为它把 VLM 从"检测者"降格成"复核者"，责任边界清楚了。但我还要求一件事：**VLM 必须输出理由，而且理由必须是可核对的结构化字段**，比如 `{缺陷类型, 位置, 估算尺寸, 判定依据, 置信度}`。理由写不出尺寸的，一律降级人工。

**乙**：可以。而且我们可以反过来用这条：**VLM 的一致性率本身就是它的"健康指标"**。如果连续 100 条难例，VLM 的三次投票一致率从 90% 掉到 60%，说明来料分布变了，该报警让工程师介入了，而不是继续盲信伪标签。

**甲**：这个我喜欢，**它把"AI 的不确定性"变成了一个可监控的产线指标**。这个可以进监控面板。

---

### 第 7 轮 —— 门控：凭什么让新模型上线？

**甲**：现在谈我最在意的部分：**门控。** 我的原则很简单——**在产线上，一个"可能更好"的模型，比一个"确定还行"的模型危险得多。** 因为前者会让你放松警惕。所以上线必须过四道关。

**第一关：指标双达标。** 过检率和漏检率**同时**达标，不允许用漏检换过检。单丝现在 recall 94.4%、过检 41%，如果新模型 recall 掉到 92% 但过检降到 25%，算不算改进？**我认为要业务方签字才算**，因为漏检和过检的成本不对称——漏检流出到客户是索赔，过检只是内部复检人力。这个比例每个行业不一样，必须让业务定，技术不能替他定。

**乙**：同意，而且指标本身也要改。现在只看 recall 是不够的，我建议加**"分层召回"**：按缺陷尺寸分层（大/中/小）、按场景分层（高/低密度）、按批次分层。整体 recall 涨了，可能只是大模型把容易的大缺陷检得更好了，小缺陷反而退了。

**甲**：**第二关：回归集分层冻结。** golden set 不能只有一个，要按工位、场景、批次分层，每层独立看指标。而且 golden set 要**定期更新但版本化**——它自己也会过期，但每次更新都要留版本，指标必须对得上版本号，不然"我用 v3 的集子测出 v5 的模型更好"这种话没法追溯。

**乙**：补一点：**golden set 里必须有"曾经失败过的样本"**。就是历史上漏检过、过检过的那些图，它们是最有价值的回归样本。每次发现一次现场误判，就把那张图加进 golden set 的"耻辱柱"分区，新模型必须先在这儿证明自己。

**甲**：这个好，**现场每一次事故都应该变成一条永久的回归测试**。
**第三关：非功能指标。** 节拍（单帧推理耗时 P99）、显存峰值、模型文件大小、加载耗时。新模型大了一倍，推理慢了 30 ms，20 个工位累计就是节拍不达标。这些和精度一样重要，甚至更容易被忽略。

**乙**：这条同意。另外我想加一个**"稳定性指标"**：同一张图连续推理 10 次，结果必须完全一致（确定性推理）。如果你用了任何带随机性的东西（dropout、数据增强的 test-time augmentation、非确定性算子），这一关就过不了。

**甲**：**第四关：灰度与自动回滚。** 5% → 50% → 100%，每档观察期至少覆盖一个完整生产班次。每档都要有**自动回滚触发条件**：NG 率异常跳变、复检工位的一致性下降、人工复核量突增。触发就自动切回上一版，同时报警。

**乙**：我在算法侧补一个：**影子期的"分歧监控"**。灰度期主模型和影子模型同时推理，记录它们的分歧率。分歧率本身是"这个模型还有多少不确定性"的度量，比任何离线指标都实时。分歧率降到某个阈值以下，才说明新模型真的学会了，可以进下一档灰度。

**甲**：好。四关加一条，我总结成一句可以贴墙上的话：**没有自动回滚的上线，不叫灰度，叫赌博。**

---

### 第 8 轮 —— 合规与追溯：谁签字？

**甲**：聊到这儿我要拔高一层。工业检测和互联网推荐最大的区别是：**推荐错了顶多用户划走，检测错了是召回、是索赔、可能是安全事故。** 所以模型变更在质量体系里是**受控变更**。汽车行业 IATF 16949、锂电客户审厂，人家第一句就问："你上次改模型是什么时候？改了什么？验证记录呢？谁批准的？"

**乙**：这意味着"在线自学习"在合规层面是个**天然逆风**的概念。审计员听到"模型自己在学习"，第一反应是"那我怎么保证它昨天和今天判得一样？"

**甲**：所以我们要换个说法，也是换个做法：**对外不叫"自学习"，叫"受控的模型迭代流程（Controlled Model Iteration）"。** 每一次变更都有：变更原因（哪批难例触发）、变更内容（权重文件 SHA256 + 训练数据清单哈希 + 超参）、验证证据（golden set 各层指标 + 灰度期分歧率曲线）、批准人、生效时间、回滚点。

**乙**：这个我认，而且我意识到一件事：**QDV 现有的 `ModelManager` 其实已经把基础设施铺好了**——manifest.json 里有 SHA256、有版本字段、有 loadable 状态、有 load_error；删除用的是 rename-to-`.trash` 的事务策略。缺的只是把"训练数据清单哈希 + 验证报告 + 批准人"这几个字段塞进去，它就能从"模型注册表"升级成"模型变更台账"。

**甲**：对，**这是最省事的落地点：不改架构，只扩字段。** 每次训练产物注册时，强制写入 5 个字段：`data_manifest_hash`、`golden_set_version`、`metrics_json`、`approver`、`rollback_of`。缺任一字段，`registerTrainedModel()` 直接拒绝注册。这样"自学习"在账本上就是一串有据可查的版本链，而不是一团在后台偷偷变化的黑箱。

**乙**：而且这条还能反过来帮我们：**回滚不再需要"重训一个旧模型"，只需要把 manifest 里的指针挪回去。** 秒级回滚。

**甲**：顺便说个我一直想说的：**判定的可复现性**。客户复检一颗料，问"你们为什么判它 NG"，我们必须能答出 "2026-09-01 14:23，模型 v7（SHA256: a3f9…），conf 阈值 0.28，检出 2 处压痕，尺寸 0.31 mm 和 0.24 mm，均超协议 0.2 mm"。这就要求**每一次判定都要落盘完整的推理上下文**，不是只存一个 OK/NG。QDV 现在 `InferenceCache` 的 key 里有 `modelPath|mtime|inputSize|conf|imgFingerprint`——这个设计思路是对的，把它从"缓存键"扩成"判定指纹"，存进结果记录，追溯就有了。

**乙**：代价是存储。全量存图很贵。

**甲**：不用全量存。**存指纹 + 存难例 + 存抽检**。正常判定的图按 7 天滚动删，难例和被人工改判过的永久保留——因为它们就是下一轮的训练数据和 golden set 的候补。

---

### 第 9 轮 —— 落到 QDV：分几步走，每步多少钱

**乙**：那我们把它落到 QDV 上。我给一条演进路线，你从工程角度砍。

| 阶段 | 内容 | 依赖现状 | QDV 侧改动 | 预期收益 |
|---|---|---|---|---|
| **S1 · L0 参数自适应** | 阈值分位数锚定、光照漂移补偿、漂移报警 | `InferenceEngine` 已有 conf/iou 参数；`ImagePreprocessor` 已有灰度统计 | 新增一个 `DriftMonitor` + 参数热更通道，**不动推理主链** | 换型/老化导致的指标骤降减少，阈值不再人工试 |
| **S2 · L1 原型记忆** | embedding 提取 + 分桶特征库 + kNN 检索 + core-set 维护 | `models/clip`、`models/zero_shot` 已在；`InferenceCache` 可复用为 embedding 索引 | 新增 `PrototypeBank` 模块 + 一个"查库/入库"算子 | 重复图推理加速；冷启动零样本；难例自动富集 |
| **S3 · 裁判与闭环** | 难例队列 → VLM 预标注 → QDVAnnotator 人工复核 → 数据集导出 | QDVAnnotator 标训闭环已通（差 T6 一键回灌） | 新增"难例工作台"视图；`sync_obb_weight.bat` 补齐 | 人工复核量下降；训练数据自动积累 |
| **S4 · L2 影子训练** | 班后增量训练（冻结骨干 + EWC + replay） → golden set 门控 → 影子 → 灰度 | `TrainingBridge` 已有 QProcess + 心跳；`DatasetValidator` 可扩为上线门禁 | `TrainingBridge` 增"增量模式"；`ModelManager` 扩台账字段；新增灰度开关 | 模型天级进化，指标持续爬升 |
| **S5 · 教练层** | LLM 做超参建议 / 失败归因 / 上线评审报告生成 / 需求翻译 | 算子元数据完整（70 个） | 纯外围工具，不进主链 | 调试门槛下降 |

**甲**：这个顺序我基本同意，L0 → L1 → L2 是对的，因为它**风险递增、价值也递增**，但每一阶段都能独立交付价值，不做完 S4，S1–S3 也已经回本了。我要补三条硬约束：

1. **S2 的 embedding 提取必须走独立线程。** 20 个工位、单帧 100 ms，主干推理已经吃满了。embedding 提取和 kNN 检索可以和推理并行，但绝不能阻塞判定路径。**L1 命中走快路径，L1 未命中不能比现在更慢。**
2. **S3 的 VLM 调用必须有预算上限。** 按难例比例设硬闸，比如每天最多送 200 张，超了就降级为"只入队不标注"。不然一次来料异常，难例暴涨，账单和延迟都爆。
3. **S4 的训练时钟必须锁死。** 只允许在"非生产时间窗"启动，且启动前检查 GPU 空闲、页面文件余量、磁盘余量。我们被页面文件坑过一次，不许坑第二次。

**乙**：这三条我都接受。我再补一个算法侧的约束：**S4 的第一次训练必须是"全量重训 + 增量微调"双跑对比。** 因为增量训练很容易在数据量小的时候不如全量重训。跑两遍，谁好在 golden set 上谁上。等难例累积到一定规模（比如超过原训练集的 15%），增量才稳定优于全量。

**甲**：合理。那我们再明确一下**不做什么**，我觉得这比做什么更重要：

**不做清单（红线）**
1. **不在生产节拍内对主模型做反向传播。** 学习发生在影子侧和时间窗外。
2. **不让 VLM 直接产出进判定链的 OK/NG。** 它只是裁判和预标注。
3. **不跨工位、跨场景混训或混检索。** 分桶是硬性的——高密度/低密度、不同相机、不同光源，各自独立。
4. **不用合成数据做验证集，也不让它在训练集中占比超过 20%。**
5. **不自动放宽阈值。** L0 只许向更严的方向自动调整。
6. **不允许无批准人的模型上线。** `registerTrainedModel()` 缺字段即拒绝。
7. **不删 golden set 的"耻辱柱"分区。** 历史事故的样本永久保留。

---

### 第 10 轮 —— 收敛：我们到底在哪里达成了一致

**乙**：回头看，我们其实在最开始问的那个问题上已经一致了：**"便捷性"不是一个点，是两件事——让第一次配置变简单（大模型翻译层 + 零样本冷启动），和让后续维护变简单（自学习闭环）。** 前者降的是入门门槛，后者降的是长期成本。你不能只要一个。

**甲**：对。我一开始只看到前者，因为我天天在救火；你一开始只看到后者，因为你在想三年后的事。现在拼起来是完整的。

**乙**：而"在线自学习"这件事，我们也达成了新的定义。它不是一个动作，是**一个三层结构 + 一个门控体系**：

> **L0 参数自适应（秒级，只调数） → L1 原型记忆（毫秒级，只加记忆） → L2 权重更新（天级，影子训练 + 四关门控）**
>
> 大模型的角色分别是：L0 的**漂移归因助手**、L1 的**难例裁判**、L2 的**数据编排教练**；全程**不进判定链**。

**甲**：我最后再强调一次那个核心判断：**L1 是被低估的那一层。** 它零遗忘、可解释、可撤销、能冷启动，而且它天然适配工业"海量重复、少量新颖"的数据特性。如果我们只做一件事，我应该做 L1，而不是急着做 L2。

**乙**：但我要保留我的分歧：**L1 的天花板比 L2 低。** 记忆能覆盖"见过的和近似见过的"，但遇到真正的新缺陷类型，它的 embedding 空间根本没有对应的区域，检索 nearest neighbor 只会给出一个错误的近邻。这时候必须靠 L2 真正改变特征提取能力。

**甲**：这个分歧我认为不用解决，因为**它们本来就该共存**：L1 负责把 90% 的重复问题吃掉并且吃稳，L2 负责啃剩下 10% 的新花样。L1 让 L2 的训练数据变干净（因为难例队列是 L1 筛出来的），L2 让 L1 的 embedding 空间变好（新模型训完，重新刷一遍特征库）。**它们是互相喂养的。**

**乙**：那就这么定。最后一句话总结我的立场：**大模型给工业检测带来的最大变化，不是让模型变准，而是让"让模型变准"这件事不再需要专家常驻。**

**甲**：我接一句：**而且必须以一种审计员能睡着觉的方式。**

---

## 三、结论汇总

### 3.1 达成的共识

| # | 共识 | 关键机制 |
|---|---|---|
| C1 | 「便捷性」= **降低配置门槛** + **降低维护成本** 两件事，缺一不可 | 大模型翻译层（前者）+ 三层自学习闭环（后者） |
| C2 | 自学习是一个**谱系**而非一个动作，按风险分三层：L0 参数 / L1 记忆 / L2 权重 | 只有 L2 涉及梯度，且必须在影子侧、时间窗外 |
| C3 | **L1 原型记忆被严重低估**：零遗忘、可解释、可撤销、支持零样本冷启动 | 冻结骨干取 embedding → 分桶特征库 → kNN 三档处置（命中/交叉验证/难例） |
| C4 | 大模型**永不直接进 OK/NG 判定链**，只做翻译、裁判、教练、诊断 | VLM 只做"小模型出候选 → 裁剪后裁判"，输出结构化字段 + 理由 |
| C5 | 标签经济学：下游反馈（趋势监督）+ VLM 伪标签（预标注）+ 人工复核（金牌）+ 合成（负样本增强） | 三次投票一致性作为 VLM 自身健康指标 |
| C6 | 四关门控：指标双达标 → golden set 分层回归 → 非功能指标 → 灰度 + 自动回滚 | 加"影子期分歧率"作为实时健康信号 |
| C7 | 每一次现场事故 = 一条永久回归样本（golden set 的"耻辱柱"分区） | 事故驱动的回归集增长 |
| C8 | 对外表述为**「受控模型迭代」**而非「自学习」；模型注册必须带 5 个追溯字段 | `data_manifest_hash` / `golden_set_version` / `metrics_json` / `approver` / `rollback_of` |
| C9 | 自动化链条每一环都要有门禁（历史上 177 个空标签训出 mAP=0 的教训） | 复用并扩展 `DatasetValidator` 的四维校验 |
| C10 | 演进顺序 L0 → L1 → L2 → 教练层，每阶段独立可交付 | 见 §3.3 |

### 3.2 保留的分歧

| # | 分歧点 | 甲（工程派） | 乙（算法派） | 处置 |
|---|---|---|---|---|
| D1 | L1 与 L2 的能力天花板 | L1 性价比最高，只做一件事应做 L1 | L1 遇真正新缺陷会给出错误近邻，必须靠 L2 | **共存互补**：L1 筛难例喂 L2，L2 训完回刷 L1 特征库 |
| D2 | 漏检与过检的取舍权重 | 属业务决策，技术不得代为决定 | 建议优化 F-beta 由业务给 beta | 由业务方按行业索赔/复检成本定 beta，写进方案配置 |
| D3 | 合成数据的价值 | domain gap 风险大，不进主训练集 | 用于负样本多样性增强可收紧决策边界 | 限用途（只做负样本增强）+ 限比例（≤20%）+ 禁止自证 |
| D4 | 天级更新是否算"在线" | 天级是工程能签字的上限 | 理想是分钟级 | 折中：**生产在线读数，时间准在线更新**；班后/换型窗自动训 |

### 3.3 落地路线（S1–S5，对应 QDV 现有落点）

| 阶段 | 内容 | QDV 现有落点（复用） | 新增量 | 风险 |
|---|---|---|---|---|
| **S1** | L0 参数自适应 + 漂移监控 | `InferenceEngine`（conf/iou）、`ImagePreprocessor`、监控模块 | `DriftMonitor` + 参数热更通道 | 低（不碰主链） |
| **S2** | L1 原型记忆 | `models/clip`、`models/zero_shot`（ZeroShotKit 冷启动）、`InferenceCache` 键设计 | `PrototypeBank` + 查库/入库算子 + FAISS 索引 | 中（线程与延迟，须独立线程、命中走快路径） |
| **S3** | 难例工作台 + VLM 预标注 + 闭环回灌 | QDVAnnotator 导出/训练链路、`verify/verify_export.py`、`ExportManager` | 难例队列视图 + VLM 适配层 + `sync_obb_weight.bat`（T6） | 中（成本与延迟，须设日调用硬闸） |
| **S4** | L2 影子训练 + 四关门控 | `TrainingBridge`（QProcess+心跳）、`DatasetValidator`（扩为上线门禁）、`ModelManager`（manifest/SHA256/.trash） | 增量训练模式 + 台账字段 + 灰度开关 + 自动回滚 | 高（遗忘/资源/合规，须全约束齐备才启动） |
| **S5** | LLM 教练层（需求翻译/超参/归因/评审报告） | 70 个算子元数据、`operators.json`、`ToolChainVerifier` | 纯外围工具 + JSON 输出 + 沙盒试跑 | 低（不进主链） |

### 3.4 一句话结论

> **把"让小模型边跑边学"从一句愿景，拆成 L0 调参、L1 记样本、L2 改权重三层；大模型只当翻译、裁判、教练，永不进判定链；所有变更走四关门控并留下可审计的版本链。这样，工业检测里的深度学习模型才第一次具备"越用越好用、且审计员睡得着觉"的可能。**

---

## 四、附：讨论中引用的项目事实索引

| 事实 | 来源 |
|---|---|
| `InferenceEngine` 双后端（OpenCV DNN / ONNX Runtime）、三类缓存、`ModelManager` LRU+manifest+SHA256+`.trash` | `docs\AI模块设计文档.md` §1–2 |
| `TrainingBridge` 经 QProcess 拉 Python、心跳防卡死、产物自动注册 | 同上 §2.3 |
| `DatasetValidator` 四维校验、健康分 100−30C−10E−2W、`canTrain()` 门禁 | 同上 §2.6 |
| ONNX Runtime 默认 OFF、推理层尚未真实化（P2-1） | `docs\算子库改进报告.md` P2-1 |
| 算子元数据 70 条、需与实现一致性校验 | 同上 P1-2；构建日志 `[OperatorDescriptors] loaded 70 operators` |
| 零样本能力已就位：`models/clip`、`models/grounding_sam`、`models/zero_shot` | `models\` 目录 |
| 标训闭环已通、T6 一键回灌未落地；空标签事故（177 张 0 字节 → mAP=0） | `tools\qdv-annotator\QDVAnnotator_闭环整合设计方案_2026-08-19.md` §4、§7 |
| 单丝指标：val 52 张 / GT=2358 / recall 94.444% / 漏检 5.556% / 过检 41.009%；conf 0.10→0.30 端点 1930→1737；高/低密度须分场景 | 项目记忆与实测记录 |
| 硬件：RTX 5060 8 GB、CUDA 12.8；页面文件不足是训练闪退根因；训练须用 QDV venv | `软硬件基本配置.md` |
| 20 个检测工位、单帧约 100 ms、6 个计划升级视频/时序检测 | 项目背景 |

---

# 第二部分：三方对话 —— 落地深化

## 五、三个智能体的设定（v2）

| | **甲（软件开发工程师）** | **乙（算法编写工程师）** | **丙（现场运维工程师）** |
|---|---|---|---|
| 代号 | 锚（Anchor） | 流（Flow） | 钉（Nail） |
| 主场 | 系统架构 / 代码实现 / 工程规范 / 交付可行性 | 算法设计 / 模型与策略选型 / 精度与性能 | 部署实施 / 运行监控 / 故障排查 / 现场约束 |
| 关注序 | 可维护性 > 可测试 > 可交付 > 先进性 | 精度上限 > 数据效率 > 收敛速度 | 不停线 > 好排查 > 少出差 > 功能多 |
| 思维起点 | "这东西怎么切模块、怎么写测试、怎么不拖垮主链" | "这个分布该怎么建模、梯度往哪走" | "夜班两点它炸了，我一个人怎么在十分钟内恢复" |
| 口头禅 | "耦合在哪儿？崩了谁兜底？" | "但这样精度天花板就被锁死了。" | "你们谁来现场待一周再说。" |
| 最怕的事 | 一个实验性功能把稳定主链拖下水 | 方案退化成调阈值，永远长不大 | 半夜被告警叫醒，看不懂日志，只能重启碰运气 |

**新增的共同约束（丙带来的现场事实）**

| # | 现场事实 | 对方案的冲击 |
|---|---|---|
| F1 | **产线内网物理隔离**，禁止外联，部分客户连 USB 口都封 | 云端大模型 API 不可用，裁判层必须本地化或走内网服务器 |
| F2 | 工控机配置参差：部分无独显、CPU 为低功耗型号、SSD 128–256 GB 且系统已占一半 | 训练不可在工控机上进行；存储预算需重新核算 |
| F3 | 三班倒连续生产，**没有"班后"**，每月仅有极短保养窗口 | "天级训练窗口"在部分产线不存在 |
| F4 | 一条线一天**换型 3–5 次**，换型停机窗口 5–15 分钟 | 学习状态必须按型号隔离，换型只是切桶不是清空 |
| F5 | 操作工非工程师，只会点固定按钮；夜班常无技术员 | 任何需要人工判断的步骤都要能降级为"一键" |
| F6 | 客户工艺锁定 + ECN 变更流程，模型变更常需客户批准 | 自动上线在多数项目不成立，**半自动推荐 + 人批准**才是常态 |
| F7 | 复检工位/后工序数据存在于 MES，但**鲜少回传给视觉系统** | 最硬的免费标签源目前是断的 |

---

## 六、对话正文（第二部分）

### 第 11 轮 —— 云端大模型能不能进车间？

**丙**：我先把最要命的事说了。你们第一部分聊了半天"VLM 当裁判"，我请问：**裁判在哪台机器上跑？** 我们客户车间的网络是物理隔离的，工控机连不上外网，有的连 USB 都封了，拷个文件要走保密流程。你那个"调用大模型 API"的方案，在我这儿**一步都走不通**。

**甲**：这条我认，而且它比想象的更严重——不只是网络问题，还是**合规问题**。就算客户允许联网，把产品图像传到外部 API，绝大部分甲方一票否决。

**乙**：那就在本地跑。现在 4B 级的多模态模型量化后 3 GB 左右，8 GB 显存能塞下。

**丙**：塞不下。8 GB 显存里，**检测模型推理已经占了一部分**，而且我们很多工位的工控机**压根没有独显**，只有核显。你那 4B 模型在核显上跑一张图几十秒，我要它当裁判？

**甲**：所以这里必须做一个**实时性分层**，我提议按响应要求把大模型能力切成三档：

| 档 | 响应要求 | 部署位置 | 允许的能力 |
|---|---|---|---|
| **T0 实时**（< 100 ms） | 必须在判定节拍内 | 无 LLM/VLM 参与，**只跑确定性小模型** | 无 |
| **T1 准实时**（秒~分钟） | 不阻塞产线，异步 | 本地独立进程 / 内网边缘服务器 | VLM 裁判（异步批处理）、embedding 提取 |
| **T2 离线**（小时~天） | 与生产解耦 | 内网训练服务器 / 工程师工作站 | 全量 LLM（超参建议、失败归因、报告生成、需求翻译） |

关键在于：**第一部分里我们默认 VLM 裁判是同步的，其实它根本不需要同步。** 难例图先入队，攒够一批（比如 64 张）再送裁判，裁判结果晚几十秒回来完全不影响产线节拍——因为它只影响"这条难例什么时候进训练集"，不影响这张图的判定。

**乙**：这个分层解了我的围。我要补的是：既然是批处理，我就能用**更大的模型、更慢的策略**。比如让 VLM 对同一条难例跑三次不同 prompt 做投票，或者先做一次粗判再对不确定的做一次精判（crop + 放大 + 标尺参照）。同步场景下这些全都做不了。

**丙**：等等，那"内网边缘服务器"是不是每台线都要加一台？预算谁出？

**甲**：不一定每台线都加。**一台边缘服务器可以管多条线的难例裁判和训练**，通过内网通信。工控机只负责采集和入队。这样新增的硬件成本是"每车间一台"，不是"每工位一台"。实在没预算的，退化成"工程师定期用 U 盘/内网同步难例到工作站处理"——丑，但能跑。

**乙**：我补个降级路径：**裁判缺席时，系统不能停摆。** 难例队列照常积累，只是没有伪标签；等有人来处理时，按"聚类后每簇只标代表性样本"的策略补标。这叫 **label-lazy（懒标注）**，把标注成本推迟到真正需要时。

**丙**：这个我喜欢。那 T2 那档呢？工程师工作站跑全量 LLM，我们公司有 24 GB 显存的机器，这个没问题。

**甲**：对，**T2 是最容易落地的**，因为它完全脱离产线，用的是现有硬件（你那台跑 Qwen3.8-27B 的机器就够了），而且它贡献的价值最直接——需求翻译、失败归因、评审报告。我建议**优先做 T2，它零风险**。

> **【本轮共识】**
> 1. 大模型能力按 **T0 实时 / T1 准实时 / T2 离线** 三档分层，T0 档严禁任何 LLM/VLM 介入。
> 2. **VLM 裁判改为异步批处理**，与生产节拍解耦 —— 这化解了"云端不可用 + 显存不够"的双重矛盾。
> 3. 硬件新增为"每车间一台边缘服务器"，非"每工位"；可降级为内网/人工同步的懒标注模式。
> 4. 优先落地 T2（工程师工作站跑全量 LLM），零风险且价值直接。
>
> **【待解决】**
> - Q1：本地 VLM 的选型与量化方案（4B 级 VLM 在 CPU/核显上的可行性实测）。
> - Q2：边缘服务器与工控机之间的通信协议与断网自治策略。
> - Q3：T2 档 LLM 输出如何结构化校验（防幻觉）的具体 schema。

---

### 第 12 轮 —— 难例洪水：夜班八小时谁来复核

**丙**：接着上一轮说难例队列。我给你算笔账：一条线一天跑 2 万件，如果按 5% 的不确定度阈值捕获难例，那就是**一天 1000 张**。夜班 8 小时没人看，白班技术员一天能认真看 100 张就不错了。攒一周就是 7000 张 backlog，**复标的速度永远追不上产生的速度**。你们那个闭环，第一天就堵死。

**乙**：所以我一直强调难例要**去重和排序**，不能无脑堆。两个手段：
**其一，感知去重。** 现场难例里绝大部分是同一类问题的重复——同一个气泡、同一种反光、同一个来料批次。用特征向量的聚类（比如 DBSCAN，eps 按历史距离分位数自动定）把它们并成一簇，一簇只留 1–3 个代表。实测经验上，**去重后能压掉 70%–90%**。
**其二，信息量排序。** 不是所有难例都值钱。"这个图跟库里已有的很像"价值低，"这个图开了一片新的特征空间区域"价值高。用 **k-center / BADGE** 这类 core-set 准则给每条难例打一个"新颖度分"，按分排序，只把头部送人工。

**甲**：工程上我要加**背压（backpressure）**。队列必须有硬上限，超了不是无限增长，而是按策略丢弃/降级：
- 队列 < 200：全量保留，送裁判 + 人工；
- 200 ≤ 队列 < 2000：只保留新颖度 Top-N，其余只留向量和元数据不留图；
- 队列 ≥ 2000：**只更新统计特征**（簇心、计数器），不再保留样本，同时触发告警"难例速率异常 —— 疑似来料或成像异常"。

第三条特别重要：**难例暴涨本身就是故障信号**，不该默默吞下去。

**丙**：对，你说的这个我太熟了。来料一批有问题，NG 率突然从 3% 跳到 40%，这时候根本不是模型该学的时候，是**该停下来查来料**。所以难例速率必须进监控看板。

**乙**：那我要区分两种"难例诱因"：**模型能力不足**（该学）和**外部扰动**（不该学，该修）。这个判别本身就该自动化——如果难例在特征空间里**聚成致密的新簇**，说明是新的分布（可能是新缺陷类型，值得学）；如果难例**均匀散布在所有区域**，说明整体噪声变大（光源/来料/对焦出问题，不该学，该报警）。

**甲**：这个判据太棒了，**它把"要不要学"变成了可计算的量**。我建议做成一个指标：**新颖度聚集系数（Novelty Aggregation Ratio, NAR）** = 难例簇内平均密度 / 全库平均密度。NAR 高 → 有新的东西，值得标注学习；NAR 低 → 全局退化，触发成像/来料排查。这个可以直接进监控面板。

**丙**：那我再加一条操作要求：**难例队列要能按工位、按型号、按班次筛。** 我排查的时候不想看 7000 张，我要看"3 号工位、A 型号、夜班"这 40 张。

**甲**：数据模型上就是给难例加维度标签 `{line_id, station_id, recipe_id, shift, timestamp, camera_id, 图像指纹, 特征向量, 新颖度分, 簇ID, 来源(不确定/低相似/人工改判)}`，存 SQLite + 特征向量存独立文件。这样筛选和聚类都在库里做，不用翻图片。

**乙**：补充一点：**人工改判的样本必须置顶**。复检工位把模型的 NG 改成 OK，或者把 OK 改成 NG，这是**最高价值的标签**——它直接告诉你模型错在哪。它不该跟普通难例排一个优先级，应该插队。

> **【本轮共识】**
> 1. 难例管线三段式：**去重（聚类）→ 排序（新颖度 core-set）→ 截断（背压硬闸）**，不做这三步闭环必堵。
> 2. 队列必须持久化 + 多维标签（工位/型号/班次/来源），支持运维侧快速筛选。
> 3. **人工改判样本优先级最高**，直接插队。
> 4. 新增指标 **NAR（新颖度聚集系数）**：区分"该学习的新分布"与"该排查的外部扰动"。
> 5. 难例速率异常 = 故障信号，须告警而非静默吸收。
>
> **【待解决】**
> - Q4：聚类参数（eps / 簇大小）如何免调参自适应。
> - Q5：背压丢弃策略会不会丢掉真正罕见但重要的缺陷（如"万分之一的新缺陷类型"），需设计保底通道。

---

### 第 13 轮 —— 特征库存哪、多大、怎么同步

**丙**：再问个具体的。你们说的 L1 原型记忆，特征库放哪？工控机 SSD 一共 128 G，系统加软件占了一半，剩 60 G 还要存图片和日志。你们 20 个工位，每个工位一个库，存三个月，得多大？

**乙**：我算给你听，你会发现**它小得惊人**。
- embedding 维度：CLIP ViT-B/32 是 512 维，做 PCA 降到 128 维足够保留判别信息；float16 存储 → **128 × 2 = 256 字节/条**。
- 按第 5 轮定的 core-set 上限 **2000 条/工位** → 2000 × 256 B = **512 KB/工位**。
- 20 个工位 × 单型号 ≈ **10 MB**；就算管 20 个型号，也就 200 MB。
- 加上索引开销（HNSW 约 1.5–2 倍）和元数据，撑死 **1 GB**。

**真正的存储大户从来不是向量，是图片。** 所以我的建议是：**特征库只存向量 + 128×128 缩略图 + 元数据，不存原图。** 原图走独立的大容量存储，按 7–30 天滚动清理。

**丙**：（愣）……512 KB？我以为得多大。那向量这事儿我不管了。但同步呢？20 个工位在不同机器上，A 工位学到的"这种压痕是 NG"，B 工位能用吗？

**乙**：这要分情况。**同型号、同相机、同光源、同工艺的工位可以共享**（它们的特征空间是一致的）；**跨型号、跨相机、跨光源的不能共享**——物理成像链路不同，embedding 空间不可比。这个边界必须由 `recipe` 定义，不能靠算法猜。

**甲**：那架构就清楚了，两级：
- **边缘热库（本机）**：内存常驻 FAISS 索引 + SSD 持久化副本，只含**本工位本型号**的近期高频原型，规模 ≤ 2000 条，检索亚毫秒。
- **中心冷库（车间服务器）**：按 `{型号, 相机型号, 光源配置}` 分桶的全量库，规模可到十万级；负责跨工位合并、core-set 重算、去重合并。

同步策略：**边缘 → 中心 单向上传（新原型 + 统计摘要），中心 → 边缘 下发合并后的 core-set**。断网时边缘完全自治，联网后补传。这里有个工程细节：**向量合并是幂等的**（按图像指纹去重），所以补传不会重复。

**丙**：那我要问个狠的：**如果中心库被污染了呢？** 比如某个工位学到一条错的原型，同步到中心，再下发到 20 个工位，是不是全厂一起错？

**甲**：……这个问题非常专业，而且我们第一部分确实漏了。**污染扩散是自学习系统最危险的失效模式之一**，它比单点错误严重得多，因为它会自我放大。

**乙**：算法上有三层防护：
1. **原型带来源与置信度**：人工确认过的原型权重最高，VLM 伪标的中等，模型自判的最低。下发时按置信度过滤，低置信原型不跨工位传播。
2. **新原型必须先本地验证**：一条新原型在**本工位**连续被命中 N 次且判定一致，才允许上传中心。避免"一次性噪声"污染全局。
3. **中心库定期用 golden set 做"净化"**：拿冻结的 golden set 去检索中心库，任何导致 golden set 判定错误的原型，标记为可疑并降级。

**甲**：我加工程层的一条：**中心库所有原型变更必须留版本 + 支持按时间点回滚**。就是把数据库做成 append-only 的变更日志，任何时刻能重建出"上周三的中心库长什么样"。这样万一污染扩散，能定位到是哪一次同步引入的、回滚到污染前。

**丙**：还有个更土的问题：**中心库谁负责清？** 型号淘汰了、工艺改了，那些原型就成垃圾了。

**甲**：原型加**生命周期字段**：`last_hit_at`。超过 N 天未被命中的原型自动进入"冷藏"（移出热库，仍在冷库），超过 M 天自动归档。同时 recipe 淘汰时整桶归档。这个是现成的缓存淘汰思路，不难做。

> **【本轮共识】**
> 1. **特征库只存向量 + 缩略图 + 元数据，不存原图**；规模：约 512 KB/工位，全厂 < 1 GB —— 存储不是瓶颈，图片才是。
> 2. 两级架构：**边缘热库（本工位，≤2000 条，亚毫秒）↔ 中心冷库（车间服务器，按型号/相机/光源分桶）**；断网自治、幂等补传。
> 3. **共享边界由 recipe 定义，不由算法猜测**：同型号同相机同光源可共享，否则禁止。
> 4. **污染扩散是头号失效模式**，四层防护：置信度分级 + 本地验证期 + golden set 净化 + 变更日志时间点回滚。
> 5. 原型生命周期：`last_hit_at` 冷藏 / 归档机制。
>
> **【待解决】**
> - Q6：embedding 主干选型（通用 CLIP vs 域内自监督 DINOv2 vs 检测骨干 GAP 特征）—— 需对比"零样本可用性"与"域内判别力"。
> - Q7：跨工位的"同型号"如何自动判定（相机序列号 / 光源电流 / 白平衡统计指纹）。

---

### 第 14 轮 —— 检测增量学习的背景类陷阱

**乙**：现在谈我最想谈、也是第一部分没展开的部分：**目标检测的增量学习，比分类难一个数量级，而且难在一个人人都会踩的坑上。**

分类任务里，你拿新数据微调，旧类别的样本不在新数据里，它们的 logits 会被压下去——这是普通的遗忘。但在**检测任务里更糟**：新数据里**旧类别的物体根本没被标注**，它们在训练时被当作**背景（background）**。也就是说，模型不光是"忘了"旧类，它是被**主动训练成"把这个东西判成背景"**。这是负向监督，遗忘速度快得多。经典文献里这叫 **background shift / 背景类陷阱**。

**甲**：等等，我确认一下：你的意思是，我拿一批只有新缺陷标注的图去微调，模型会**积极地**学会把旧缺陷当成正常背景？

**乙**：对。而且这不是精度缓慢下降，是**断崖式崩塌**。RPN/anchor 阶段尚且好些，检测头的分类分支会迅速把旧类的分数压到背景那一侧。所以第一部分的"冻结骨干 + EWC + replay 30%"在**检测任务上是不够的**。

**丙**：那我听懂了：**你们说的"在线学习"，对检测模型来说，风险比你们以为的高得多。**

**乙**：对，所以我要给第一部分那个"L2 权重更新"做**难度分级**，不是所有更新都叫 L2：

| 子类 | 场景 | 做法 | 风险 | 多久做一次 |
|---|---|---|---|---|
| **L2-a 域漂移适配** | 同类缺陷，光照/批次/机台漂移 | 冻结骨干，**只更新检测头 + BN 统计量**；训练数据含全量旧类别标注（replay） | 中 | 周~月 |
| **L2-b 置信度重标定** | 模型判别力没变，但 score 分布整体漂移 | **完全不改权重**：温度标定（Platt scaling / temperature scaling）+ 阈值分位数锚定 | **极低** | 天 |
| **L2-c 新类别增量** | 出现全新的缺陷类型 | 检测头加节点 + 全量重训 + 人工全量标注 | 高 | 事件驱动（不在自动流程内） |

**关键结论：在线自动化的应该是 L2-b，谨慎半自动的是 L2-a，L2-c 永远不要进自动流程。**

**甲**：L2-b 我太喜欢了，因为它**根本不碰模型**。具体怎么做？

**乙**：两步。
**第一步，温度标定。** 训练完的模型，其输出 score 通常是"过自信"的（现代网络通病）。用一个小的验证集拟合一个标量温度 T（单参数，几十张图就能拟合得很稳），让 `p' = softmax(logit / T)` 在校准集上的 NLL 最小。这一步能直接改善"score 不可信"的问题，而且**它是纯后处理，不改一个权重**。
**第二步，阈值分位数锚定。** 不要在配置里写死 conf=0.25。而是维护历史判定 score 的滑动分布，把阈值锚定到分布的分位数（比如"历史 NG 判定的 score 中位数下浮一个标准差"，或者按目标 NG 率反推分位数）。这样光照慢慢变、来料慢慢变，阈值跟着漂，指标不崩。

**丙**：等等，第一步我有个疑问：那个"小的验证集"从哪来？现场没有标注好的验证集啊。

**乙**：好问题。三个来源：① 交付时冻结的 golden set（有，但会过期）；② **复检工位的结果**（第 16 轮会详细谈，这是免费的持续标签源）；③ **L1 原型库里人工确认过的样本**（它们天然带标签，且是最新的）。③ 特别好用——**原型库既是记忆，也是标定集**，一物两用。

**甲**：L2-a 的工程约束我得说清楚。它必须满足：
- 训练数据里**必须包含全量旧类别的标注**（replay 集），比例不低于 50%，不能是 30%；
- **必须冻结骨干**，只训检测头（参数量 < 5%，也大幅降低过拟合风险）；
- 训练必须**在车间服务器**，不能在工控机（F2：工控机可能无独显）；
- 产物**必须过四关门控**（第一部分 §3.1 C6）。

**丙**：L2-c 为什么不能自动化？我们现场最常见的就是"客户新提了一种缺陷，要加上"。

**乙**：因为新类别意味着**决策边界要重画**。旧模型从来不知道这个东西存在，它在特征空间里可能跟某个旧类挨得很近。要正确分开它们，需要**同时**看到新类和旧类的样本重新训练——这是全量重训，不是增量。而且新类的标注量、标注质量、与旧类的混淆矩阵，都需要人工判断。**自动化这一块，省的时间远小于出的风险。**

**甲**：但我们可以自动化 L2-c 的**准备工作**：新类别提出来之后，系统自动去历史图库里检索"可能属于这个新类的历史样本"（用 VLM + 文本描述做零样本检索，QDV 里 `models/clip`、`grounding_sam` 现成），给标注工程师预标注一批候选。这能把"从零标 500 张"变成"确认 200 张"，省一半以上。

**丙**：这个实用。我们就干过——客户说"以后这种划痕也要判 NG"，然后翻三个月的历史图找样本，找了两天。

> **【本轮共识】**
> 1. **检测增量学习存在"背景类陷阱"**：未标注的旧类物体被当背景训练 → 断崖式遗忘，比分类严重得多。
> 2. L2 拆为三子类：**L2-b 置信度重标定（不改权重，天级，可全自动）→ L2-a 域漂移适配（只训检测头 + ≥50% replay，周/月级，半自动）→ L2-c 新类增量（全量重训 + 人工，事件驱动，绝不自动化）**。
> 3. 温度标定（单参数 Temperature Scaling）是**性价比最高的精度改进**，纯后处理、零风险，应优先落地。
> 4. 标定集三来源：冻结 golden set / 复检数据 / **L1 原型库（记忆与标定集一物两用）**。
> 5. L2-c 可自动化"准备"不可自动化"执行"：用 VLM 零样本检索历史图做预标注。
>
> **【待解决】**
> - Q8：L2-a 中 replay 比例（50%？70%？）需实验确定；检测任务是否需要额外的**蒸馏损失**（LwF/RFD）也有待验证。
> - Q9：如何自动判别当前漂移属于 L2-a（域漂移）还是 L2-c（新类别）—— 见下轮开放集。

---

### 第 15 轮 —— 开放集与"没见过"：三态输出的杀手锏

**乙**：上一轮留下的 Q9，我用这一轮回答。核心问题是：**系统怎么知道自己"没见过"这个东西？**

先说为什么 softmax 靠不住。深度学习模型对**分布外（OOD）样本会给出高置信度的错误预测**——这是 2017 年就被指出的问题。你让它看一个从没见过的缺陷，它可能 0.97 自信地说"这是 OK"。闭集 softmax 的"置信度"根本不是"见过没见过"的度量。

**丙**：这就是我最怕的。**模型信心满满地放过一个我从没见过的缺陷，比报错更可怕**，因为报错我会去看，放过了就流到客户手里了。

**乙**：所以我们要的是**开放集识别**，而 L1 的 kNN 检索天然就是最好的开放集检测器：

> 新样本 embedding → 检索最近原型 → **距离就是"陌生度"**。距离小 = 见过；距离大 = 没见过。

这个距离是可度量的、可解释的、且**不需要训练**。对比 softmax，它诚实得多。

**甲**：那阈值怎么定？你说"距离大就是没见过"，大多算大？不同工位、不同型号肯定不一样，我不接受再增加一个要人工调的参数。

**乙**：自动标定，分三步：
1. **建库时**：对特征库里所有原型，算它到自己最近邻原型的距离 d，得到距离分布 D；
2. **取分位数**：陌生度阈值 τ = D 的 **99 分位数**（即：训练时见过的样本，99% 的最近邻距离都小于 τ）；
3. **运行时**：新样本的最近邻距离 > τ → 判为 **UNKNOWN**。

随库增长自动重算，零人工。想更严就取 99.5 分位，想更松取 98——**一个可解释的旋钮，而不是一堆魔法数**。

**丙**：这个我能接受。但我要把它变成一个**输出态**，不是内部指标。判定结果必须是三态：

| 输出 | 含义 | 处置 |
|---|---|---|
| **OK** | 高置信良品 | 放行 |
| **NG** | 高置信不良 | 剔料 |
| **UNKNOWN** | **模型没见过 / 不确定** | **转人工复核**，且**不计入自动放行** |

现在我们的系统只有 OK/NG 两态，模型拿不准的时候它也得硬选一个，然后一半是错的。有了 UNKNOWN，它就能说"我不知道"，让人工看。**这才是 L1 真正值钱的地方——不是它多准，是它知道自己什么时候不准。**

**甲**：工程上这有三个连锁改动，我得说清楚代价：
1. **判定链要支持三态**，下游的 `BranchControl`、通信算子、PLC 输出都要加一个分支；
2. **UNKNOWN 必须走"安全侧"**——不允许因为 UNKNOWN 太多就自动放行，UNKNOWN 队列满时要**停机报警**而不是降级放行；
3. **UNKNOWN 率必须进监控**，它是模型健康度最灵敏的指标之一（突然升高 = 来料/成像/模型任一出问题）。

**乙**：我补个算法细节：UNKNOWN 不该只看单一阈值，应该**分两级**：
- **UNKNOWN-soft**（距离在 τ ~ 1.2τ）：走模型判定，但**标记**为低置信，采样送人工抽检（比如 10%）；
- **UNKNOWN-hard**（距离 > 1.2τ）：**完全不采信模型判定**，直接转人工。

这样能把必须人工的量压下来——只有真正陌生的才全额人工。

**丙**：那人工量怎么控？我们复检工位就那么两个人。

**乙**：靠**运营指标闭环**：设定目标 UNKNOWN 率（比如 ≤ 3%），系统自动调 τ 的分位数去逼近这个目标。**把"人工承受力"变成一个显式的系统参数**，而不是让算法自己决定。冷启动期 UNKNOWN 率高（正常，因为库是空的），随着原型积累自然下降——这条下降曲线本身就是"系统学习进度条"，可以拿给客户看。

**甲**：这个"学习进度条"我要单独拎出来说，它解决一个很现实的问题：**客户怎么感知到系统在变好？** 解释 EWC、embedding 没用，但给客户看一张"人工复核率从 30% 降到 5%"的曲线，他立刻懂了。

**丙**：对，我们给客户汇报就缺这种东西。以前只能说"我们优化了模型"，客户说"你怎么证明"，我们拿不出来。

> **【本轮共识】**
> 1. **闭集 softmax 的置信度不可用作 OOD 判据**（OOD 样本会给出高置信错误预测）；L1 的 **kNN 距离是最实用的开放集检测器**，零训练、可解释。
> 2. 判定输出从两态升级为 **OK / NG / UNKNOWN 三态**；UNKNOWN **走安全侧**（转人工，绝不自动放行），队列满则停机报警。
> 3. 陌生度阈值 τ **自动标定**（库内最近邻距离分布的 99 分位数），零人工调参；且按"目标 UNKNOWN 率"闭环微调。
> 4. UNKNOWN 分 soft（抽检）/ hard（全额人工）两级，控制人工量。
> 5. **"人工复核率下降曲线"= 学习进度条**，是向客户证明系统价值的最有效指标。
>
> **【待解决】**
> - Q10：三态输出对现有判定链 / `BranchControl` / PLC 通信的改造范围与兼容性。
> - Q11：UNKNOWN-hard 的处理延迟是否影响节拍（需异步旁路，不能阻塞主链）。
> - Q12：τ 的自动调整与"故意放过"之间的博弈（阈值被攻击/被滥用的风险）。

---

### 第 16 轮 —— 指标可比性：把复检数据变成免费的持续评估集

**丙**：聊了这么多学习，我问个打脸的问题：**你们怎么知道新模型更好？** 别跟我说 golden set，那是我们交付时挑的几十张图，三个月后早就过期了。现场没人做评估，也没人有时间做评估。我们现在的做法是——换模型靠感觉，工程师说"看着还行"，就换了。

**甲**：这个必须解决，否则前面所有工作都是空中楼阁。我提一个思路：**不要用人工评估，用生产流程中自然产生的信号。**

**乙**：对，产线上其实一直有一个**免费的标签源在流失**——**复检工位 / 后工序 / MES 的最终结果**。视觉系统判 NG，复检的人拿起来看，判"其实 OK"（过检）或"确实 NG"（确认）；视觉判 OK 的料流到后工序，如果后工序发现不良（漏检），也会被记录。**这些是真实的、带成本权重的标签，而且每天都在产生。**

**丙**：（苦笑）那个数据在 MES 里，我们视觉系统拿不到。客户的信息部门不给接口，给了的也格式对不上。这事儿我推了两年没推动。

**甲**：那就分两级拿：
- **理想路径**：跟 MES 对接，拿到复检结果回传。技术不难（一个 HTTP/MQTT 接口 + 工单号关联），难在跨部门协调。
- **降级路径（无需 MES）**：**在复检工位放一个极简反馈终端**——一块小屏 + 两个按钮（"确认 NG" / "误报 OK"）+ 扫码枪。复检员拿起料扫一下码、点两下，30 秒都用不了。**这个不需要 IT 部门，产线自己就能装。**

**乙**：降级路径的算法价值极高。它直接产出**过检率的一致估计**：
> 过检率 ≈ 复检判定"误报 OK"的数量 / 视觉判 NG 的总数

而**漏检率**虽然难直接测（OK 的料不会全检），但可以**抽检**估：每天随机抽 N 件 OK 料做人工复检。每天 20 件，一周就能得到统计上可用的漏检估计。

**丙**：抽检我们做过，但断断续续。要是系统能自动提示"今天该抽这 20 件"，而且**优先抽那些 UNKNOWN-soft 和 score 贴近阈值的**（最可能是漏检的），效率会高很多。

**乙**：这就是**主动学习的不确定性采样**了。给个极简实现：按 `|score − threshold|` 升序排，越接近阈值的越优先抽检。同样 20 件预算，随机抽可能一个漏检都碰不上，按不确定性抽能捞到好几个。

**甲**：那把指标体系定下来，**分两类**：

| 类别 | 指标 | 数据来源 | 是否需标注 |
|---|---|---|---|
| **自动指标**（零成本） | 节拍 P99、显存峰值、UNKNOWN 率、NG 率趋势、score 分布漂移、embedding 距离漂移、难例速率、NAR | 系统内部 | 否 |
| **半自动指标**（低成本） | 过检率、漏检率（抽检估计）、**人工复核一致率**、VLM 三次投票一致率 | 复检反馈终端 + 抽检 | 是，但量极小 |

**核心健康指标我提议定为「人工复核一致率」**（复检确认 NG / 视觉判 NG 总数）。理由：每天自然产生、不需额外标注、直接对应过检成本、客户也听得懂。

**丙**：但有个坑：**NG 率上升不一定是模型变差**，可能就是来料变差了。我上周就遇到一次，NG 率从 2% 涨到 15%，一查是供应商换了一批料，视觉系统反而判得挺准。

**乙**：所以指标要看**"一致率"而不是"NG 率"**。NG 率受来料影响，一致率只反映模型判得准不准——来料再差，模型判 NG 的里面"确实是 NG"的比例应该是稳定的。**这正好是我们要的归一化。**

**甲**：那监控面板里就把这两个分开画：**NG 率（业务指标，受来料影响）** 与 **一致率（模型指标，与来料无关）**。两者同时看，就不会误判。

> **【本轮共识】**
> 1. 放弃"靠冻结 golden set 评估模型"的思路，改用**生产流程中自然产生的信号**做持续评估。
> 2. 复检数据是最硬的免费标签源；MES 对接是理想路径，**复检工位反馈终端（扫码 + 两按钮）是零依赖的降级路径**，产线自装即可。
> 3. 漏检率靠**主动学习式抽检**（优先抽 score 贴近阈值的样本）用小预算估出。
> 4. 指标体系分**自动指标**（零成本）与**半自动指标**（低成本）；**核心健康指标 = 人工复核一致率**。
> 5. **NG 率 ≠ 模型健康**：NG 率受来料影响，一致率才是模型指标，两者必须分开看。
>
> **【待解决】**
> - Q13：复检反馈终端的硬件成本与产线推行阻力。
> - Q14：抽检样本量（每天 20 件？）的统计功效验证 —— 漏检率估计本身的方差有多大。

---

### 第 17 轮 —— 四维体检与故障树：是模型坏了还是灯坏了

**丙**：这一轮说我最大的痛点。现场判不准了，我要花一两个小时才能搞明白是哪出问题：可能是光源衰减、相机松动、镜头脏了、来料变了、机械定位偏了，也可能是模型废了。**我要的是"一眼定位"，不是给我一堆日志。**

**甲**：那就做**分层健康检查 + 故障树**，把排查路径固化成系统能力。分四层，**自底向上查，下层异常先修下层**：

| 层 | 检查对象 | 具体指标（均可自动计算） | 异常时结论 |
|---|---|---|---|
| **L1 成像层** | 相机、光源、镜头、机械 | ROI 内灰度均值/方差趋势、**拉普拉斯方差**（模糊度）、**FFT 频谱**（条纹/工频干扰）、模板匹配偏移（定位漂移）、坏点脏点计数 | **八成问题在这层** |
| **L2 数据层** | 输入分布 | 图像统计分布漂移（PSI / 直方图距离）、**UNKNOWN 率**、embedding 距离分布漂移、NAR | 来料或工艺变了 |
| **L3 模型层** | 模型本身 | score 分布漂移、过检/漏检/一致率、**VLM 投票一致率**、温度标定参数漂移 | 模型能力退化或标定失效 |
| **L4 系统层** | 运行环境 | 节拍 P99、显存/内存、GPU 温度、磁盘余量、丢帧率、进程存活 | 资源或硬件故障 |

**乙**：L1 那层我补充算法细节，这几个指标都是**无参考图像质量评价**，不需要标准图：
- **模糊/失焦**：拉普拉斯响应的方差。对焦良好时高频丰富、方差大；失焦急剧下降。镜头松半圈就能看出来。
- **光照衰减**：ROI 内灰度均值的滑动趋势。**关键是要有基准**——交付时记录一次"健康态"基准值，之后看相对变化。绝对值没意义，相对值才有。
- **条纹/干扰**：对 ROI 做 FFT，看特定频率能量峰。工频干扰（50 Hz）会落在固定空间频率上，一眼可辨。
- **定位漂移**：用夹具标记点做模板匹配看偏移量——这个 QDV 的 `FindShapeModel` 现成就能干。

**丙**：这些我都要。特别是"光照衰减趋势"——**我们最常见的事故就是光源慢慢变暗，所有人都不觉得有问题，直到某天 NG 率突然爆掉。** 要是有条曲线提前一个月告诉我"同轴光已衰减 18%"，我能省好几次出差。

**甲**：那就把**衰减趋势 + 预测**做进去：基于历史拟合衰减斜率，外推"预计 Y 天后跌破阈值"，提前生成维护工单。技术上线性回归就够，但价值极大——**把被动救火变成主动保养**。

**乙**：我补一个更高阶的：**层间关联诊断**。单看一层容易误判，比如"UNKNOWN 率上升"（L2）可能是成像变差（L1）引起的，也可能是模型能力不足（L3）。判据就是**看 L1 是否同时漂移**：L1 正常而 L2 漂 → 真新分布（该学习）；L1 与 L2 一起漂 → 成像问题（该修灯）。

**丙**：这条太实用了，直接写进排查手册：**先看成像是好的，再看是不是模型的事。**

**甲**：落地形式我建议是**一键体检**：运维点一个按钮，系统 30 秒内跑完四层检查，输出一张表（每层：正常/警告/异常 + 具体数值 + 与基准偏差 + 建议动作）。再加**每日自动快照**存历史看趋势。

**丙**：还要能**导出报告**。客户审厂问"你们怎么保证设备一直正常"，我把体检报告一甩，比什么都有说服力。

> **【本轮共识】**
> 1. 建立**四层健康检查**（成像 → 数据 → 模型 → 系统），**自底向上排查**，下层异常优先修下层。
> 2. 成像层用**无参考图像质量评价**：拉普拉斯方差（模糊）、灰度均值趋势（光衰）、FFT 频谱（干扰）、模板匹配偏移（定位）—— 全部自动计算，无需标准图。
> 3. **所有趋势指标必须记录交付时的"健康态基准"**，看相对变化而非绝对值。
> 4. **层间关联诊断**：L1 正常 + L2 漂 = 真新分布（该学）；L1/L2 同漂 = 成像问题（该修灯）。
> 5. 落地形态：**一键体检（30 秒出表）+ 每日自动快照 + 可导出报告**（兼作客户审厂材料）。
> 6. 加分项：光衰斜率外推 + 预测性维护工单。
>
> **【待解决】**
> - Q15：各层阈值的初始基准如何标准化（不同工位/型号/客户健康态不同）。
> - Q16：体检自身的性能开销（FFT + 模板匹配是否影响节拍 —— 须在低优先级线程 / 换型间隙执行）。

---

### 第 18 轮 —— 换型：一天四次的配方切换

**丙**：接着问个天天发生的事。一条线一天换 3–5 次型，有的客户更多。每次换型参数全变——ROI、阈值、光源亮度、模型、特征库，全都不一样。**你们那套"学习"的东西，换型之后怎么办？全废了重来？**

**甲**：不废，因为**所有学习状态本来就按 recipe 分桶**。架构上定义：

```
Recipe（配方） = 型号 ID + 工位 ID + 相机 ID
   ├── 检测配置（算子链、ROI、阈值、光源参数）
   ├── 模型版本指针（manifest 中的 model_id + SHA256）
   ├── L1 原型库（本 recipe 独有，与其他 recipe 隔离）
   ├── L0 参数状态（当前 conf 阈值、温度标定 T、归一化统计量）
   ├── golden set 版本指针
   └── 学习历史（难例统计、NAR 曲线、变更台账）
```

**换型 = 切换 recipe，不是清空学习状态。** 换回来时，上次学到的东西还在。

**丙**：那新 recipe 第一次上线呢？库是空的，按你们说的冷启动期 UNKNOWN 率会很高，人工扛不住。

**乙**：三个手段叠加，把冷启动期压到最短：
1. **零样本冷启动**：用 VLM / CLIP 直接给出初始判定能力（`models/zero_shot`、`models/clip` 现成），第一天就有 60–70 分可用，不是 0 分。
2. **跨 recipe 迁移**：若新 recipe 与某已有 recipe 属"同族"（同产品系列、同材质、同相机型号），**复制其原型库作为初始库**再快速修正，能省掉大半冷启动期。
3. **首件检验即标签源**：换型后前 N 件（比如 20 件）按工艺要求本来就要人工全检——**把这次全检结果直接注入原型库**，冷启动从"一周"变"一天"。

**丙**：对，首件检验是强制工艺，你们直接用现成的，我一点额外工作量都没有。这个好。

**甲**：安全策略上，新 recipe 冷启动期必须**降级运行**：
- 阈值**更严**（宁可过检不可漏检）；
- UNKNOWN 阈值 τ 的**分位数更高**（更容易判 UNKNOWN → 更多人工复核）；
- **禁止任何自动权重更新**（L2-a 在冷启动期不启动）；
- 满足"学习成熟度"后才解除降级，转入正常模式。

**乙**：成熟度判据要量化，建议三条件**同时**满足：① 原型库条数 ≥ N（覆盖度）；② 近 3 天 UNKNOWN 率 < 阈值（陌生度收敛）；③ 近 3 天人工复核一致率 > 阈值（准确性）。

**丙**：还有个情况：**型号迭代**。客户改了设计，同一个型号代码，产品变了。这时候旧原型库反而是毒药。

**甲**：那就需要 **recipe 版本 + 失效机制**。型号版本变更（ECN 触发）时提示"是否为新版本"，选择"新版本"则**归档旧库、开新桶**，而不是继续往旧库里加。归档库保留可查，但不参与检索。

**乙**：算法侧可以自动检测"悄悄变了"的情况：**用 PSI / MMD 监控 embedding 分布**，若发生**阶跃跳变**（不是渐变）且跳变后旧原型命中率断崖下跌，则告警"疑似产品改版，请确认是否为新版本 recipe"。

> **【本轮共识】**
> 1. 引入 **Recipe（配方）** 作为所有状态的隔离单元（`型号 + 工位 + 相机`）；**换型 = 切桶，不清空**。
> 2. Recipe 打包检测配置 / 模型指针 / 原型库 / L0 状态 / golden set 指针 / 学习历史，可整体导出备份与跨设备复制。
> 3. 冷启动三手段：**零样本冷启动（VLM/CLIP）→ 同族 recipe 迁移 → 首件检验结果直接注入**。
> 4. **冷启动期降级运行**：更严阈值 + 更高 UNKNOWN 敏感度 + **禁止自动权重更新**；满足成熟度三条件后解除。
> 5. 产品改版由 ECN 或**分布阶跃自动检测**触发 recipe 版本升级，旧库归档不参与检索。
>
> **【待解决】**
> - Q17：Recipe 的持久化格式与迁移/复制工具（跨设备部署 recipe 是刚需）。
> - Q18：同族 recipe 的自动判定（相似度阈值）与迁移后的负迁移风险。

---

### 第 19 轮 —— 代码架构：单向数据流、独立进程与一键降级

**甲**：前面聊得都很好，现在我说软件实现，这是我最担心的部分。**核心原则只有一条：学习系统绝不能把检测主链拖下水。**

**1）进程隔离。** 学习相关的一切（难例采集、embedding、检索、训练、VLM 裁判、门控）放进**独立进程 `LearningService`**，通过本地 IPC（Qt Remote Objects，或 localhost HTTP + 共享内存传图）与 QDV 主程序通信。
- 好处一：**LearningService 崩溃不影响检测**，检测端退化为"无学习模式"继续跑；
- 好处二：显存/内存与主进程隔离，训练 OOM 不连带；
- 好处三：可独立启停、独立升级。

**2）单向数据流（防腐层）。** 数据流必须严格单向，禁止回环：

```
[检测主链] ──(图 + 上下文 + 判定结果)──► [难例采集] ──► [去重/排序] ──► [裁判标注]
                                                                          │
                                          ┌───────────────────────────────┘
                                          ▼
                                   [原型库 / 训练集]
                                          │
                                   [L2 训练 + 四关门控]
                                          │
                             [模型仓库 manifest] ──(仅加载新权重)──► [检测主链]
                             [L0 参数服务] ──(阈值/温度 T/归一化)──► [检测主链]
```

**唯一两个回写点：模型权重加载、L0 参数下发。** 且二者都必须经过**校验 + 版本化 + 可回滚**。学习服务**永远不能反向调用检测算子**，它只能"提议"，由检测端"采纳"。

**乙**：等等，我要的"主动学习式抽检"（第 16 轮）需要检测端配合——我要它把某些样本优先送人工。这算不算反向调用？

**甲**：不算，只要你走**声明式**而非命令式：学习服务发布一条**策略**（"今日抽检策略：score 落在 [0.42, 0.58] 的样本采样率 20%"），检测端自行执行并上报。学习服务不直接控制检测行为，只发布参数。这是防腐层的关键——**用数据耦合代替控制耦合**。

**3）状态机 + crash-safe。** 学习流程有状态，必须显式建模：

```
IDLE → COLLECTING → JUDGING → (LABELING) → TRAINING → SHADOW → CANARY(5%→50%→100%) → ACTIVE
         │                                     │           │          │
         └─────────────────────────────────────┴───────────┴──────────┴──► ROLLBACK
```

每个状态**持久化到 SQLite**，进程重启能从中断处恢复。特别是 `TRAINING` 和 `CANARY`——半夜断电，第二天开机必须知道"昨天训到一半"或"正灰度到 50%"，不能重来也不能停在半路。

**丙**：我要加一条：**一键降级开关。** 现场出问题，运维要能在 5 秒内把整个学习系统关掉，系统回到"纯检测"模式——就像这功能从来没装过。而且关掉后**已有判定结果不受影响**。

**甲**：同意，做成**三级开关**（不是简单开/关）：

| 级别 | 行为 | 适用场景 |
|---|---|---|
| **Level 0 · 全关** | LearningService 停止，检测端不采集不查库 | 学习系统故障、客户要求禁用 |
| **Level 1 · 只观测** | 采集 + 统计 + 体检告警，**但不改任何参数、不上任何模型** | 新线磨合期、客户审厂期间（**出厂默认**） |
| **Level 2 · 全自动** | 完整闭环，含 L0 自适应与 L2 影子训练 | 成熟产线 |

**乙**：我唯一要争取的：**Level 0 下 L1 检索也停吗？** 因为 L1 命中走快路径是有性能收益的。

**甲**：全停，保持行为一致性和可预测性。**降级模式越简单越好排查**，不留半开半关的状态。性能收益在 Level 1 就给。

**丙**：还有个运维要求：**学习系统的日志要独立**，跟检测日志分开。我排查时不想在一堆检测日志里翻训练记录。

**甲**：对，独立日志文件 + 独立日志级别 + 结构化（JSON Lines）便于检索上报。再加一条：**每一次"改变系统行为的动作"（改阈值、换模型、更新原型库）都必须写审计日志**——时间、动作、触发原因、前后值、证据（指标快照）。

> **【本轮共识】**
> 1. **进程隔离**：学习系统 = 独立 `LearningService` 进程，崩了不影响检测。
> 2. **单向数据流（防腐层）**：学习端只能"提议"，检测端"采纳"；**用数据耦合代替控制耦合**（发布策略而非下达命令）。
> 3. 唯一两个回写点：**模型权重加载** 与 **L0 参数下发**，均须校验 + 版本化 + 可回滚。
> 4. **显式状态机 + SQLite 持久化**，crash-safe，重启从断点恢复。
> 5. **三级降级开关**（全关 / 只观测 / 全自动），**出厂默认 Level 1 只观测**；Level 0 全停保证行为可预测。
> 6. 独立结构化日志 + **行为改变审计日志**（时间 / 动作 / 原因 / 前后值 / 证据快照）。
>
> **【待解决】**
> - Q19：IPC 选型（QtRO / localhost HTTP / 共享内存）与跨进程图像传输的性能实测。
> - Q20：`LearningService` 与现有 `TrainingBridge`（QProcess 拉 Python）是复用还是重构。
> - Q21：灰度流量切分在检测链上如何实现（按件计数？按时间片？）。

---

### 第 20 轮 —— 工期、成本与优先级重排

**丙**：最后一轮，我说点实在的。这套东西我听下来觉得好，但我得问：**什么时候能在我这条线上跑起来？要多少人？会不会拖累现在的项目？**

**甲**：我按模块估工作量（单位：人周，含开发与测试），并标注依赖与风险：

| 模块 | 内容 | 工作量 | 依赖 | 风险 |
|---|---|---|---|---|
| **M1 可观测性底座** | 四层体检、指标看板、一键体检报告、审计日志 | 4–6 | 无（纯增量） | 低 |
| **M2 Recipe 化** | 配置/状态按 recipe 分桶、导入导出、切换 | 3–4 | 配置系统改造 | 中 |
| **M3 复检反馈闭环** | 反馈终端/接口 + 一致率指标 + 抽检策略 | 2–3 | M1 | 低 |
| **M4 L0 自适应** | 漂移监控、温度标定、阈值分位锚定 | 3–4 | M1、M3 | 中（要动判定链参数） |
| **M5 L1 原型记忆** | embedding、分桶特征库、kNN 三态、core-set | 6–8 | M2、Q6 选型 | 中高 |
| **M6 难例工作台** | 队列、去重、排序、背压、VLM 裁判、复核 UI | 6–8 | M5、Q1 选型 | 中高 |
| **M7 L2 影子训练** | 增量训练、四关门控、灰度、回滚、台账 | 8–12 | M4、M5、M6 | 高（合规约束） |

**丙**：加起来三十多到五十人周，小半年。太久了，我们等不起，而且风险全在后面。

**乙**：那我提一个**优先级重排的建议**，这也是我这几轮最大的转变：

**我原本认为 L2（真·权重更新）是核心价值，现在我认为它不是。** 理由有三：
- L2 风险最高、工作量最大，且在多数项目里**受合规限制根本不能自动上线**（F6：客户 ECN 流程）；
- 而 **M1 + M2 + M3** 加起来 9–13 人周，就能交付丙最想要的两件事——"**停机时告诉我为什么**"和"**换型后别让我重调一遍**"；
- 更关键的是，**M1/M2/M3 是 M5/M6/M7 的前提**。没有可观测性，你无法判断学习是否有效；没有 recipe 化，学习状态无处安放。**先把地基打了，后面是增量叠加，每一期都能独立交付价值。**

**甲**：我完全同意，还要强调：**M1 可观测性现在就能做，它不依赖任何算法结论，也不动判定主链，风险接近于零。** 而且它有个隐藏收益——**它能立刻告诉我们"现在这套系统到底有多不准"**。我们现在是凭感觉说"过检 41%"，装上 M1+M3 之后，我们会有真实的、持续的现场指标。**先量出来，再谈优化。**

**丙**：我加一句：**M3（复检反馈）可能比你们想的更值钱。** 现场最缺的不是算法，是"到底哪些判错了"这个事实。现在全靠客户投诉和定期抽检，滞后一两个月。有了复检终端，当天就知道。

**乙**：而且 M3 直接产出**训练数据**。复检终端每天点几百次，一个月就是上万条真实标注——比我们费劲做半自动标注靠谱得多。**它是数据飞轮的入口。**

**甲**：那最终路线重排为四期：

| 期 | 模块 | 周期 | 交付价值 | 可独立验收 |
|---|---|---|---|---|
| **P1 · 先量出来** | M1 可观测性 + M3 复检反馈 | ~6–9 人周 | 现场真实指标；故障一键定位；复检数据回流 | 是 |
| **P2 · 别重调** | M2 Recipe 化 + M4 L0 自适应 | ~6–8 人周 | 换型零重调；阈值自动跟漂；光衰预测维护 | 是 |
| **P3 · 记住它** | M5 原型记忆 + M6 难例工作台 | ~12–16 人周 | 三态判定；人工复核量下降曲线；冷启动能力 | 是 |
| **P4 · 才谈学习** | M7 影子训练与门控 | ~8–12 人周 | 模型天级进化（受合规约束，多数项目停在半自动） | 是，且可选 |

**P1 先上；P2/P3/P4 视 P1 的实测数据再决定是否投入。**

**丙**：这个我能接受。**先给我一个能看懂现场发生了什么的工具，比给我一个会自动变聪明但我不敢用的黑箱重要得多。**

**乙**：我保留立场：**P4 不该被砍掉，只是该被推后。** 前三期积累的原型库、难例集、复检标签，恰恰是让 P4 真正可行的唯一途径。**没有 P1–P3 铺路，P4 是空中楼阁；有了它们，P4 水到渠成。**

**甲**：收尾总结我的三条工程判断：
1. **先观测，再优化** —— 没有测量就没有改进；
2. **先隔离，再闭环** —— 没有边界的自动化是灾难；
3. **先有降级能力，再有自动能力** —— 能一键关掉的功能才敢上线。

> **【本轮共识】**
> 1. **优先级重排：可观测性与 Recipe 化先于自学习。** 丙最痛的"故障定位"与"换型重调"恰恰是 P1/P2 就能解决的，无需等算法突破。
> 2. 四期路线 **P1 先量出来 → P2 别重调 → P3 记住它 → P4 才谈学习**，每期独立可交付、独立可验收、可随时止步。
> 3. **M3 复检反馈是数据飞轮的入口**，价值被低估：既是指标源，也是标注源。
> 4. P4（权重自动更新）**推后但保留**，多数项目受合规限制会停在"半自动推荐 + 人批准"。
> 5. 工程三原则：**先观测再优化 / 先隔离再闭环 / 先有降级能力再有自动能力**。
>
> **【待解决】**
> - Q22：P1 立项与资源（人周排期、与现有 QDV 迭代计划的冲突）。
> - Q23：是否先在**单丝计数**或**电芯蓝膜六面检测**之一做 P1 试点（建议前者：数据、指标、闭环工具现成）。

---

## 七、第二部分终版结论

### 7.1 三方共同签署的共识（v2 终版）

| # | 共识 | 主要提出方 | 相对第一部分的变化 |
|---|---|---|---|
| **V1** | 大模型按 **T0 实时 / T1 准实时 / T2 离线** 三档分层；**T0 档严禁 LLM/VLM 介入**；VLM 裁判改为**异步批处理** | 甲提出、丙质疑、乙补充 | **修正**：第一部分默认裁判同步，实则不可行 |
| **V2** | 硬件新增为"**每车间一台边缘服务器**"而非每工位；可降级为懒标注 | 甲 | 新增成本模型 |
| **V3** | 难例管线三段式：**去重 → 排序 → 截断**；新增 **NAR 指标**区分"该学的新分布"与"该修的外部扰动" | 乙提出、甲加背压、丙加速率告警 | 新增 |
| **V4** | 特征库**只存向量 + 缩略图**，约 512 KB/工位，全厂 < 1 GB；两级架构（边缘热库 / 中心冷库），断网自治 | 乙算账、甲定架构 | 量化确认：存储非瓶颈，图片才是 |
| **V5** | **污染扩散是自学习系统头号失效模式**，四层防护（置信分级 / 本地验证期 / golden set 净化 / 时间点回滚） | 丙提出、甲乙补方案 | **第一部分遗漏的重大风险** |
| **V6** | 检测增量学习存在**背景类陷阱**；L2 拆为 **L2-b 重标定（全自动）→ L2-a 域适配（半自动）→ L2-c 新类（绝不自动化）** | 乙 | 第一部分 L2 颗粒度不足 |
| **V7** | **温度标定（单参数）** 是性价比最高的精度改进，纯后处理、零风险，应最优先落地 | 乙 | 新增 |
| **V8** | 判定输出升级为 **OK / NG / UNKNOWN 三态**；陌生度阈值 τ 自动标定（库内最近邻距离 99 分位数）；UNKNOWN 走安全侧 | 乙提出、丙要求、甲补工程约束 | 两态判定升级 |
| **V9** | 评估体系从冻结 golden set 转向**生产自然信号**；核心指标 = **人工复核一致率**；**NG 率 ≠ 模型健康** | 丙质疑、甲乙共建 | **替换第一部分的评估假设** |
| **V10** | **四层健康检查 + 层间关联诊断**（L1 正常而 L2 漂 = 该学；L1/L2 同漂 = 该修灯） | 甲分层、乙补算法、丙补场景 | 新增 |
| **V11** | 引入 **Recipe** 作为状态隔离单元；换型 = 切桶；冷启动三手段 + 降级运行 + 成熟度门禁 | 甲定义、乙补迁移、丙补首件检验 | **第一部分遗漏换型场景** |
| **V12** | **进程隔离 + 单向数据流 + 状态机 crash-safe + 三级降级开关（出厂 Level 1）** | 甲 | 新增工程架构 |
| **V13** | **优先级重排：可观测性与 Recipe 化先于自学习**；四期 P1–P4 独立可交付 | 甲估算、乙反思、丙拍板 | **对第一部分 S1–S5 顺序的重大调整** |
| **V14** | **人工复核率下降曲线 = 学习进度条**，是向客户证明系统价值的最有效指标 | 乙提出、丙确认 | 新增 |

### 7.2 未决问题清单（Q1–Q23）

| # | 问题 | 归属 | 阻塞模块 |
|---|---|---|---|
| Q1 | 本地 VLM 选型与量化（4B 级在 CPU/核显上的实测） | 乙 + 甲 | M6 |
| Q2 | 边缘服务器与工控机的通信协议、断网自治策略 | 甲 | M6 |
| Q3 | T2 档 LLM 输出的结构化校验 schema | 甲 | M1 / M6 |
| Q4 | 聚类参数（eps / 簇大小）免调参自适应 | 乙 | M6 |
| Q5 | 背压丢弃与"万分之一罕见缺陷"的保底通道设计 | 乙 + 丙 | M6 |
| **Q6** | **embedding 主干选型：通用 CLIP vs 域内 DINOv2 vs 检测骨干 GAP 特征** | 乙 | **M5（关键路径）** |
| Q7 | 跨工位"同型号"的自动判定（相机/光源指纹） | 甲 + 丙 | M5 |
| Q8 | L2-a 中 replay 比例与是否需蒸馏损失（LwF/RFD） | 乙 | M7 |
| Q9 | 域漂移（L2-a）与新类别（L2-c）的自动判别 | 乙 | M7（部分由 V8 缓解） |
| Q10 | 三态输出对判定链 / `BranchControl` / PLC 通信的改造范围 | 甲 | M5 |
| Q11 | UNKNOWN-hard 处理的异步旁路设计（不阻塞节拍） | 甲 | M5 |
| Q12 | τ 自动调整的滥用风险（被故意放松） | 甲 + 丙 | M5 |
| Q13 | 复检反馈终端的硬件成本与推行阻力 | 丙 | M3 |
| Q14 | 抽检样本量的统计功效验证 | 乙 | M3 |
| Q15 | 各层体检阈值的初始基准标准化 | 乙 + 丙 | M1 |
| Q16 | 体检自身的性能开销（须低优先级线程执行） | 甲 | M1 |
| Q17 | Recipe 持久化格式与跨设备复制工具 | 甲 | M2 |
| Q18 | 同族 recipe 自动判定与负迁移风险 | 乙 | M2 |
| Q19 | IPC 选型与跨进程图像传输性能 | 甲 | 全部 |
| Q20 | `LearningService` 与现有 `TrainingBridge` 是复用还是重构 | 甲 | M7 |
| Q21 | 灰度流量切分方式（按件 / 按时间片） | 甲 | M7 |
| **Q22** | **P1 立项与排期，与现有 QDV 迭代的冲突** | 三方 + 业务 | P1 |
| **Q23** | **试点项目选择**（建议单丝计数：数据、指标、闭环工具现成） | 三方 + 业务 | P1 |

### 7.3 三方各一句话总结

> **甲（软件开发）**：先隔离、再闭环，**能一键关掉的功能才敢上线**。可观测性和 Recipe 化是所有"自学习"的地基 —— 没有它们，学习就是黑箱里的赌博。
>
> **乙（算法）**：模型不必靠改权重变强。**记忆（L1 原型）、标定（L2-b 温度缩放）、开放集（UNKNOWN）** 这三件事加起来，比任何增量训练都更快见效、更安全；权重更新排在最后，而且必须站在前三者的肩膀上。
>
> **丙（现场运维）**：我不要一个会自动变聪明但我看不懂的系统。我要的是"**出问题一眼定位**"、"**换型不用重调**"、"**这个月比上个月少看多少张图**"。这三件事做到了，AI 才算真的落了地。

---

*本文档为方案探讨记录，未执行任何编译、未修改任何现有代码。*


