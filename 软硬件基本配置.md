# 《软硬件基本配置》 — QDV 项目环境速查

> 完整性：2026-08-21 实测生成 | 适用范围：本机（华硕 天选Air）上 QDV / QDVAnnotator / 标训闭环 / 单丝参考工具
> 用途：交接与排障一键对照。任何重装、扩容、改库后请更新本表。

---

## 一、硬件配置

| 项 | 规格 | 备注 |
|---|---|---|
| 系统 | Windows 11 家庭中文版 64 位 Build 26200 | |
| 机型 | ASUSTeK 天选Air FA401KM_FA401KM | |
| CPU | AMD Ryzen AI 7 H 350（含核显 Radeon 860M） | 8 核 16 线程 |
| 内存 | DDR 31.1 GB | |
| 独显 | **NVIDIA GeForce RTX 5060 Laptop GPU**，显存 **8151 MiB（8GB）**，TGP 66W | WDDM 模式 |
| 显卡驱动 | NVIDIA 572.97 / **CUDA 12.8** | |
| 核显 | Radeon 860M（CPU 集成） | |

### 磁盘空间（重要告警）
| 盘符 | 总容量 | 剩余 | 评价 |
|---|---|---|---|
| C: | 175.8 GB | **17.8 GB** | ⚠️ **紧张**：系统页面文件位于 C 盘，空间不足是训练 `WinError 1455 页面文件太小` 闪烁退根因 |
| D: | 260.4 GB | 49.6 GB | 充裕（软件默认装 D 盘） |
| E: | 244.1 GB | 29.5 GB | 充裕（项目/数据在这） |

---

## 二、训练环境（QDVAnnotator「标训闭环」专用）

- **解释器**：`E:\anchor\Trae\QDV\training\venv\Scripts\python.exe`
- **Python**：3.11.9（venv 基盘 `D:\qdv_temp\Python311`，`include-system-site-packages=false` 纯隔离）
- **配置位置**：[TrainingDialog.qml](file:///e:/anchor/Trae/QDV/tools/qdv-annotator/qml/TrainingDialog.qml#L126-L136) 第 135 行硬编码默认路径（无持久化，可在窗口改+「检测」验证）
- **脚本**：`E:\anchor\Trae\QDV\training\yolo_train.py`

| 库 | 版本 | 说明 |
|---|---|---|
| PyTorch | **2.7.1+cu128** | CUDA 12.8 / cuDNN 9.7.1（`torch.cuda.is_available=True`，识别 RTX 5060） |
| ultralytics | 8.4.121 | YOLO 训练/导出 |
| opencv-python | 5.0.0.93 | |
| numpy | 2.4.6 | |
| onnx / onnxruntime | 1.22.0 / 1.29.0 | |
| onnxsim / onnxslim | 0.7.3 / 0.1.96 | 导出精简 |
| Pillow | 12.3.0 | |

### ⛔ 环境红线（训练用错了会导致死锁/失败）
- **训练必须用上面的训练 venv**，不能用全局 `D:\Program Files\Python314\python.exe`（其 torch 在 Qt 子进程 import 会死锁）。
- 训练参数会**自适应**：按 8GB 显存 clamp 批量、按可用页面文件自动降档（`_tune_resources`）；`TrainingDialog` 默认批大小 8。

---

## 三、全局 Python 环境（单丝参考工具 / 8765 服务）

- **解释器**：`D:\Program Files\Python314\python.exe`
- **Python**：3.14.3
- **用途**：`annotator_rect2.html`（单丝计数参考标注）、`auto_label_server.py`（127.0.0.1:8765 自动标注服务）

| 库 | 版本 |
|---|---|
| PyTorch | **2.11.0+cu128**（CUDA 12.8，可用） |
| ultralytics | 8.4.70 |
| opencv(-contrib/headless) | 4.13.0.92 |
| onnx / onnxruntime | 1.21.0 / 1.26.0 |
| fastapi | 0.139.0 |
| uvicorn | 0.48.0 |

---

## 四、QDVAnnotator 构建环境（Windows 应用）

| 项 | 配置 |
|---|---|
| Qt | **6.11.1**（`D:\Qt_new\6.11.1\mingw_64`） |
| C++ 编译器 | **MinGW 13.10 64 位**（`D:\Qt_new\Tools\mingw1310_64\bin\c++.exe`） |
| 构建系统 | CMake + Ninja（目录 `tools\qdv-annotator\build`） |
| 构建命令 | `cmake --build build --target QDVAnnotator` |
| 运行插件 | QML 组件需在 `qmldir` + qrc + 引用处三处登记 |
| 运行时 | 需设 `QSG_RHI_BACKEND=d3d11`、`QSG_RENDER_LOOP=basic`（防 QRhi 纹理错误/闪屏） |

> QVD 主程序（主项目 `QDV`）另有一套独立训练/推理模块（`TrainingBridge`/`TrainingClient`），与 QDVAnnotator 相互隔离，配置不共用。

---

## 五、网络端口

| 端口 | 用途 | 启动方 |
|---|---|---|
| 8765 | 自动标注服务 `auto_label_server.py` | 全局 Python314（人工/脚本启动） |

---

## 六、关键风险与待办

| # | 风险/事项 | 建议动作 |
|---|---|---|
| 1 | **C 盘剩余小 + 页面文件不足** → 训练闪退根因 | 在 Windows「虚拟内存」设置中把页面文件改为**系统管理**或 ≥16GB，并定期清理 C 盘 |
| 2 | 训练必须用 venv，别误用全局 Python314 | 见 §二红线 |
| 3 | 显存仅 8GB | 默认已适配（batch≤8 / 自动降档）；如要提升速度先扩虚拟内存再手动调大 batch |
| 4 | 安装/迁移软件默认放 D 盘，尽量少占 C 盘 | 本项目已遵循 |

---

## 附：本表来源（命令速查）
```powershell
Get-CimInstance Win32_OperatingSystem / Processor / ComputerSystem / VideoController
nvidia-smi
<venv-python> -c "import torch;print(torch.__version__,torch.version.cuda)"
<venv-python> -m pip list
Get-PSDrive C,D,E
qmake -query QT_VERSION      # D:/Qt_new/6.11.1/mingw_64/bin/qmake.exe
```