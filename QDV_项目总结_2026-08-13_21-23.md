# QDV（QDetectVision）项目总结 —— 面向接手智能体的开发指南

> **文档类型**：项目交接 / 总体总结（Handoff Summary）
> **生成时间**：2026-08-13 21:23 (GMT+8)
> **生成视角**：算法应用工程师 + 视觉算法团队主理人
> **适用对象**：后续接手本项目的智能体 / 开发者（快速建立全局认知、避免踩已知坑）
> **配套深度文档**：`docs/optimization/QDV_System_Test_And_Optimization_Report.md`（436 行测试优化报告）

---

## 0. 一句话定位

QDV（QDetectVision）是一个 **C++ / Qt6 / QML 的工业机器视觉一体化平台**，对标海康 SCMVS，集成「方案编辑 + 算子链编排 + 相机采集 + AI 推理」四大能力。核心资产是 `src/Vision/` 下 **70 个视觉算子** 与 `ToolChainExecutor` 执行引擎。

---

## 1. 技术栈与版本（务必对齐，否则编译失败）

| 项 | 版本 / 路径 | 备注 |
|----|------------|------|
| 语言 | **C++17** | `set(CMAKE_CXX_STANDARD 17)` |
| 框架 | **Qt 6.11.1**（mingw_64） | 路径 `D:\Qt_new\6.11.1\mingw_64` |
| 编译器 | MinGW `mingw1310_64` | `D:\Qt_new\Tools\mingw1310_64` |
| 构建 | CMake + Ninja / MinGW Makefiles | `cmake --build . -j 8` |
| 视觉库 | **OpenCV 4.13**（MinGW 源码编译） | 根 `D:\opencv\build_mingw`；world 库名 `opencv_world4130`；宏 `HAS_OPENCV` |
| 推理 | ONNX Runtime / OpenVINO / CUDA | 见 `OperatorSDK/IInferenceEngine.h`；`models/` 下 16 个 onnx |
| 数据 | SQLite（`Qt6::Sql`） | 方案/结果持久化 |
| 测试 | Catch2（`tests/`，`QDV_tests.exe`） | GUI 子系统，命令行直接跑会静默退出，需 `QT_QPA_PLATFORM=offscreen` |

> ⚠️ **坑**：OpenCV 必须是 **MinGW 编译版**，不能用 MSVC 预编译库混链。Qt 路径写死在 `build.ps1` 里，换机器要先改该脚本。

---

## 2. 系统架构（6 层）

```
┌──────────────────────────────────────────────────────────────┐
│ L1 入口        SmartVision.exe / main.cpp                      │
├──────────────────────────────────────────────────────────────┤
│ L2 界面层      Qt6 QML：LoginView / RunView / EditView / Monitor│
├──────────────────────────────────────────────────────────────┤
│ L3 业务层(C++) SchemeManager · ToolChainExecutor · OutputController │
│               CameraManager · CommunicationManager · ModelManager    │
├──────────────────────────────────────────────────────────────┤
│ L4 服务线程    CameraGrabThread · ImageProcessThread ·          │
│               TrainServerThread · FileAccessThread · AsyncOperate│
├──────────────────────────────────────────────────────────────┤
│ L5 SDK/算法    OpenCV 4.13 · ONNX Runtime · OpenVINO · CUDA     │
├──────────────────────────────────────────────────────────────┤
│ L6 硬件/系统   GigE Vision · TCP/UDP · Serial · SQLite          │
└──────────────────────────────────────────────────────────────┘
```

### 2.1 核心数据流
```
相机采集(cv::Mat) → ToolChainExecutor(串行/并行调度 70 算子) →
   每个算子输出 ToolResult{ ok, data, score, ports, overlayImage } →
   BranchControl/OutputControl 分支与输出 → QML 渲染 overlay
```
- **ToolResult** 是算子间契约（见 `include/Core/VisionTool.h` / `ToolResult`）。
- 算子通过 `m_inputs` / `m_outputs` 端口连接，执行引擎按拓扑顺序驱动。

### 2.2 算子全景（70 个，全部已核实一致）
| 分类 | 数量 | 代表 |
|------|------|------|
| 预处理 | 13 | GaussFilter、MedianImage、ImagePreprocess、FftGeneric |
| 形态学 | 6 | Erosion/Dilation/Opening/Closing/TopHat/BottomHat |
| 特征提取 | 6 | EdgesSubPix、PointsHarris、Harris |
| 深度学习 | 6 | AiClassify、YoloDetect、SegmentDL、DetectObjectsDL、DLOCR、ZeroShotDetect |
| 匹配测量 | 若干 | TemplateMatch、FindShapeModel、FindNccModel、Caliper、GeometryMeasure |
| 几何/变换 | 若干 | AffineTransImage、PolarTransImage、DistancePp、AngleLl |
| 分析识别 | 若干 | Histogram、QrCodeDetect、Barcode1d、Ocr、HandEyeCalib、RobotPose |
| 控制 | 若干 | BranchControl、OutputControl、PositionCorrect、UnitConvert |

> **已验证**：`config/operators.json` 的 70 条元数据 与 `ToolFactory.cpp` 注册 **100% 一致，无幻影算子**（P0-1 历史问题已修复）。

---

## 3. 目录结构与关键文件

```
E:\anchor\Trae\QDV\
├── CMakeLists.txt                     # 构建入口（OpenCV/Qt/ONNX 配置）
├── build.ps1 / build_D盘.bat / build_D盘.ps1  # 构建脚本（改 Qt 路径先改这里）
├── src/Vision/                        # ★核心：所有算子实现 (.cpp/.h)
│   ├── ToolFactory.cpp                # ★算子注册中心（新增算子必须改这里）
│   ├── ToolChainExecutor.cpp          # ★算子链执行引擎
│   ├── CaliperTool.cpp                # 卡尺测量（本次优化过）
│   ├── FindShapeModelTool.cpp         # 形状匹配（本次优化过）
│   ├── TemplateMatchTool.cpp          # 模板匹配（本次优化过）
│   ├── LineCircleDetectTool.cpp       # 圆/线检测（本次优化过）
│   ├── ImagePreprocessTool.cpp        # 预处理（本次优化过）
│   ├── SurfaceDefectTool.cpp          # 表面缺陷（仍有隐患）
│   ├── BlobDetectTool.cpp / ThresholdTool.cpp / DynThresholdTool.cpp
│   └── ...（其余算子）
├── include/Vision/                    # 算子头文件（与 src 一一对应）
├── include/Core/VisionTool.h          # 算子基类 + ToolResult 定义
├── OperatorSDK/IInferenceEngine.h     # 推理引擎抽象接口
├── config/operators.json              # ★算子元数据（UI 参数/默认值/分类）
├── qml/                               # QML 界面（27 个 .qml）
├── apps/                              # 旧版/辅助应用（含 .cpp/.txt/.qss）
├── models/                            # 16 个 onnx + 配置（AI 算子权重）
├── 测试图/                            # 7 张 1200×1200 8bpp 灰度 BMP（金属件）
├── tests/                             # Catch2 单元测试（CMakeLists + test_main.cpp）
├── docs/                              # 文档（含 optimization/ 本次报告）
├── auto_test/                         # 历史测试日志（9 个 .log）
└── .tmp/                              # 本次测试脚本与 JSON 结果（可复现）
```

---

## 4. 构建与运行（接手第一步）

```powershell
# 方式一：PowerShell 完整重建（会删除旧 build）
cd E:\anchor\Trae\QDV
.\build.ps1

# 方式二：增量构建（已配好 CMake 后）
cd build && cmake --build . -j 8

# 跑 Catch2 测试（GUI 子系统，必须 offscreen 否则静默退出）
$env:QT_QPA_PLATFORM="offscreen"
.\build\bin\QDV_tests.exe
```

**现有测试套件 `QDV_tests.exe`** 在命令行会静默退出，原因是 EXE 是 GUI 子系统——**不是测试逻辑问题**，用 `QT_QPA_PLATFORM=offscreen` 即可正常输出。

---

## 5. 如何新增一个算子（给其他智能体的最常用操作）

1. 在 `include/Vision/` 新建 `XxxTool.h`，继承 `VisionTool`（见 `include/Core/VisionTool.h`）。
2. 实现核心方法：`configure()`、`execute(input, result)`、`serialize()`、`deserialize()`。
3. 在 `src/Vision/XxxTool.cpp` 实现；`type()` 返回唯一字符串（如 `"Xxx"`）。
4. **在 `ToolFactory.cpp` 顶部 `#include "XxxTool.h"`**，并在 `registerTools()` 中 `registerTool<XxxTool>("Xxx")`。
5. **在 `config/operators.json` 追加一条元数据**（type 必须与注册字符串一致，否则会成为「幻影算子」被 UI 列出却无法实例化）。
6. 编译验证；补一条 `tests/` 用例。

> ⚠️ **一致性铁律**：`operators.json` 的 `type` ↔ `ToolFactory::registerTool<>` 字符串 ↔ `type()` 返回值，三者必须完全一致。

---

## 6. 本次已完成的缺陷修复与优化（2026-08-13）

| # | 严重度 | 位置 | 问题 | 修复 | 效果 |
|---|--------|------|------|------|------|
| 1 | P0 | `CaliperTool.cpp` | 工业软边（渐变圆周）场景梯度低于阈值，**7/7 图全部失效** | 加 `cv::Sobel` 梯度 + 自适应阈值降级 + `useSobel`/`autoThreshold` 开关 | 通过率 **0% → 28.6%**，亮度 ±20 完全稳健 |
| 2 | P0 | `FindShapeModelTool.cpp` | 默认参数暴力枚举尺度×角度×位置，单帧潜在 **50s** | 早停（`score≥0.97` 提前结束）+ 元数据默认 `angleExtent=0`/`scaleStep=0.2` | 平均 **1.94x** 加速（最大 3.32x） |
| 3 | P0 | `TemplateMatchTool.cpp::loadTemplate` | `cv::imread` 不兼容 GBK 中文路径，加载崩溃 | 改用 `QFile` + `cv::imdecode` | 中文路径模板可加载 |
| 4 | P0 | `LineCircleDetectTool.cpp::deserialize` | 缺失 `dp/minDist/param1/param2` 反序列化，参数丢失 | 补全 4 个字段反序列化 | 圆/线检测参数持久化正确 |
| 5 | P1 | `ImagePreprocessTool.cpp` | `bilateralFilter` 参数硬编码 | 新增 `bilateralD`/`bilateralSigmaColor`/`bilateralSigmaSpace` 可配 | 双边滤波可调 |
| 6 | 元数据 | `config/operators.json` | Caliper 默认阈值 30 偏高、FindShapeModel 默认 360° 搜索 | 默认阈值 30→5、angleExtent 360→0、scaleStep 0.1→0.2 | 开箱即用更合理 |

**测试结果**：77 用例（功能 41 + 性能 17 + 鲁棒性 19），通过率 **61.0%**；元数据一致性 100%。

**复现脚本**（Python 3.13 + OpenCV 4.13 + NumPy，位于 `.tmp/`）：
```bash
D:\Program Files\Python314\python.exe .tmp/test_framework.py          # 完整测试
D:\Program Files\Python314\python.exe .tmp/test_caliper_optim.py      # Caliper 优化对比
D:\Program Files\Python314\python.exe .tmp/test_findshape_optim.py    # FindShapeModel 优化对比
```
结果 JSON：`.tmp/test_results/full_test_results.json`、`caliper_optimization.json`、`findshape_optimization.json`。

---

## 7. 测试样本特征（写算法时务必参考）

7 张 `测试图/*.bmp` 均为 **1200×1200 / 8bpp 灰度** 工业圆形金属件：
- 整体偏暗（mean≈34）、**低对比度**（std≈40）、低噪声
- **边缘弱**（梯度 p99≈10），圆周是**渐变带**（非硬边），单点梯度仅 ≈2.92
- 表面细密磨砂纹理 + 顶部阴影干扰
- 真实缺陷：麻点 / 暗色划痕（表面缺陷检测典型场景）

> 这意味着：**默认阈值型算子（Caliper/Canny/Blob）需要低阈值或自适应阈值**；圆周测量必须用软边友好算法（Sobel + 亚像素），硬边差分法会失效。

---

## 8. 已知未决问题与下一步建议（留给后续智能体）

| 优先级 | 问题 | 建议方案 |
|--------|------|----------|
| P1 | `SurfaceDefectTool` 无 ROI 限定，背景误检 | 加 ROI 裁剪 + 自适应阈值 |
| P1 | `BlobDetectTool` 仅用面积过滤 | 启用 颜色/凸度/inertia 多维过滤 |
| P2 | OpenCV 直接传播 NaN 像素 | 算子入口加 NaN/inf 防御 |
| P2 | `Canny` 固定阈值 | 改 Otsu/中值自适应阈值 |
| P2 | `FindShapeModelTool` 仍是暴力枚举 | 图像金字塔（预期 4–10x 加速） |
| P2 | `CaliperTool` 单尺度 Sobel | 多尺度 σ=1/3/5 融合 |
| P3 | `DLOCR/AiClassify/YoloDetect` 后端为桩 | 接真实 ONNX Runtime 推理 |

---

## 9. 重要约定与坑（务必记牢）

1. **算子三处一致性**：`operators.json` type ↔ `ToolFactory` 注册 ↔ `type()`，三者一致。
2. **中文路径**：文件读取一律走 `QFile`+`cv::imdecode`，禁止裸 `cv::imread`。
3. **形态学核**：`kernelSize` 必须 ≥1 且为奇数，偶数/0 会触发 OpenCV `normalizeAnchor` 断言崩溃（已在 `ImagePreprocessTool::configure` 做防御，新增算子需自保）。
4. **GUI 测试静默退出**：跑 `QDV_tests.exe` 必须 `QT_QPA_PLATFORM=offscreen`。
5. **OpenCV 必须是 MinGW 编译版**，路径在 `build.ps1` 写死。
6. **已验证无幻影算子**，但新增算子后务必复核 `config/operators.json` 与 `ToolFactory` 同步。

---

## 10. 接手建议路线

1. 先读 `docs/optimization/QDV_System_Test_And_Optimization_Report.md` 了解完整测试结论。
2. 跑 `build.ps1` 确认本地环境可编译（先核对 Qt/OpenCV 路径）。
3. 用 `.tmp/test_framework.py` 复现基线指标，作为后续优化对照。
4. 优先处理第 8 节 P1 项（SurfaceDefect ROI、Blob 多维过滤）。
5. 任何算子改动遵循第 5 节「新增算子」流程与第 9 节约定。

---
*本总结由算法应用工程师完成系统测试与优化后生成，旨在降低后续智能体/开发者的接手成本。所有优化改动均保留向后兼容（旧方案文件可正常加载）。*
