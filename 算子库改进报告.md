# 算子库改进报告：QDV 算子能力补全与架构优化

> **目标**：针对《对比分析报告》揭示的差距，按优先级给出可落地的算子补充、接口优化与性能提升方案。
> **原则**：先消除崩溃风险与刚需缺口（P0），再做架构健壮性（P1），最后性能与精度（P2）。每项标注：动作 / 涉及文件 / 预期收益。
> **核实状态（2026-07-15）**：对标对象（海康 MVS/VM）的能力清单已据公开来源交叉核实，原 20 个 `(推断)` 算子全部确认为真实能力，故本报告所列差距均属**已核实的真实差距**；仅 4 处子项存疑（双边滤波、独立 Clip、Lua 脚本语言、部分数值默认值），不影响本报告的优先级判定与落地路线。

---

## 优先级总览

| 优先级 | 主题 | 项数 | 核心价值 |
|---|---|---|---|
| **P0** | 消除运行时风险 + 补齐工业刚需算子 | 6 | 止血 + 解锁产线基本可用 |
| **P1** | 接口与数据契约健壮性 | 5 | 防错、可维护、可扩展 |
| **P2** | 性能与精度提升 | 5 | 节拍达标、亚像素、GPU |

---

## P0 — 紧急修复与刚需算子补全

### P0-1 清除 3 个幻影算子（运行时崩溃止血）
- **问题**：`Reconstruct3D` / `BinocularDisparity` / `SurfaceMatching` 在 `operators.json` 有元数据、`AgentOperatorRouter.cpp` 有引用，但**无 `*Tool.cpp`、未在 `ToolFactory` 注册**，`createTool()` 返回 `nullptr`。
- **动作（二选一）**：
  1. 暂不下架：在 `ToolFactory::createTool` 对未注册 type 返回带 `ToolResult.ok=false` 的标准错误（而非 `nullptr`），UI 禁用该算子并提示"未实现"；
  2. 下架：从 `operators.json` 删除这 3 条，待真正实现后再加回。
- **涉及文件**：`src/Vision/ToolFactory.cpp`、`config/operators.json`、`src/Core/AgentOperatorRouter/AgentOperatorRouter.cpp`
- **收益**：消除拖入即崩溃的体验事故。

### P0-2 新增「位置修正 PositionCorrect」算子（产线刚需）
- **问题**：无 ROI/测量框随匹配位姿跟随机制，任意姿态工件测量误差大。
- **动作**：新增 `PositionCorrectTool`，输入上游匹配 `Pose(X,Y,Angle,Scale)`，输出仿射变换矩阵；下游算子（测量/ROI）读取该矩阵做坐标映射。参考 `GeometryMeasure` 的 `pixelScale` 思路扩展为完整 2D 仿射。
- **涉及文件**：`include/Vision/PositionCorrectTool.h/.cpp`、`ToolFactory.cpp`（注册）、`operators.json`
- **收益**：解锁"任意姿态稳定测量 / 机器人纠偏"，对齐 VM D.9。

### P0-3 新增「单位换算 UnitConvert」算子（物理坐标闭环）
- **问题**：测量只能输出像素值，无标定驱动的物理尺寸。
- **动作**：新增 `UnitConvertTool`，消费相机标定文件（内参+畸变，由 P0-5 的 CameraCalib 产出），将 `GeometryMeasure`/`DistancePp` 的像素结果转换为 mm；与 `HandEyeCalib` 共用标定数据池。
- **涉及文件**：新增工具 + `ToolFactory` + `operators.json`；扩展 `GeometryMeasure`/`DistancePp` 支持"输出单位"参数（px/mm）。
- **收益**：检测报告具备工程意义，对齐 VM J.3。

### P0-4 新增「卡尺测量 Caliper」算子（亚像素宽距）
- **问题**：尺寸检测靠 Hough 圆/线，精度不足。
- **动作**：新增 `CaliperTool`，实现双卡尺边缘对查找（投影+极性+滤波尺寸+边缘阈值），输出亚像素间距；复用 `EdgesSubPix` 的边缘提取能力。
- **涉及文件**：新增工具 + 注册 + `operators.json`。
- **收益**：Cover Glass 宽度、孔间距等尺寸检测达到亚像素，对齐 VM E.1。

### P0-5 新增「相机标定 CameraCalib」+ 修正分类
- **问题**：有手眼标定但无相机内参标定文件算子；`HandEyeCalib`/`RobotPose` 被误归入「特征提取」。
- **动作**：新增 `CameraCalibTool`（张正友法 `calibrateCamera`，输出标定文件供 UnitConvert）；将 `HandEyeCalib`/`RobotPose` 迁移至新分类「标定与坐标」。
- **涉及文件**：新增工具 + `operators.json` 分类修正 + `OperatorDescriptors` 分类逻辑。
- **收益**：标定数据闭环，分类语义正确。

### P0-6 补齐缺失的匹配/识别关键算子
- **动作**（按性价比排序）：
  1. `HighPrecisionMatch`：在 `FindShapeModel` 增加**尺度搜索**（各向异性 ScaleX/ScaleY），对齐 VM D.2；
  2. `ContourMatch`：基于轮廓的形状描述子匹配（抗形变），或强化 `FindShapeModel` 为真正轮廓法；
  3. `ColorMatch`：标准样片颜色比对（HSV 直方图/模板），对齐 VM F.2（2026-07-15 核实 VM 支持 RGB/HSV/HSL/Lab/YUV 多色彩空间，比对可用多空间统计量）；
  4. `DLOCR`：端到端文本检测+识别（当前 `Ocr` 仅识别）；
  5. `LightControl`：外部光源通道控制（IO 触发/频闪），对齐 VM A.2。
- **涉及文件**：相应新增/扩展工具 + 注册 + `operators.json`。
- **收益**：覆盖 VM 核心匹配/颜色/文本/光源能力。

---

## P1 — 接口与数据契约健壮性

### P1-1 执行失败语义修正
- **问题**：`ToolChainExecutor::execute()` **恒返回 true**，工具失败不中断，违背"FAIL 阻断"原则。
- **动作**：`execute()` 返回 `bool`（任一工具 `ok=false` 且非 `BranchControl` 则整体失败）；`ToolResult.ok` 驱动链路；保留"分支容错"语义。同步修复 `ToolChainVerifier` 缺失的 `verifyAiClassify` / `verifyReadImage`。
- **涉及文件**：`src/Vision/ToolChainExecutor.cpp`、`src/Vision/ToolChainVerifier.cpp`
- **收益**：运行结果可信，便于产线判定。

### P1-2 元数据—实现一致性校验
- **问题**：`operators.json`（58）与 `ToolFactory`（55）不同步，易再生幻影算子。
- **动作**：启动时 `OperatorDescriptors::all()` 与 `ToolFactory::getAvailableToolTypes()` 交叉校验，缺失实现的元数据标灰/隐藏并写日志告警；提供 `--verify-operators` 自检命令。
- **涉及文件**：`src/UI/OperatorDescriptors.cpp`、`main.cpp`
- **收益**：架构演进不再出现"元数据有、实现无"。

### P1-3 显式类型化数据端口（替代隐式 overlayImage 总线）
- **问题**：`ToolResult.overlayImage` 既当可视化又当输入，无 schema，下游类型错误只能运行时发现。
- **动作**：为 `ToolResult` 增加 `QVariantMap ports`（typed：Image/Region/Pose/Contour/String…），`EditViewBridge` 连接线校验端口类型兼容；保留 `overlayImage` 用于渲染。
- **涉及文件**：`include/Core/VisionTool.h`、`EditViewBridge`、`qml/EditView`
- **收益**：编译/连線期即可发现类型不匹配，降低方案错误率。

### P1-4 方案内流程控制补全（Script / Loop / Variable）
- **问题**：仅有 `BranchControl`，无循环（多 ROI）、脚本、变量计算。
- **动作**：新增 `LoopTool`（遍历多 ROI/多目标）、`ScriptTool`（表达式引擎，语言路线建议 **C#/VB 或 Python**；注：VM 脚本语言为 C#/VB、Python 走自定义模块，"Lua" 实为**存疑**，故 QDV 宜避开 Lua）、`VariableTool`（数值/字符串运算+格式化）；`BranchControl` 扩展比较算子集。
- **涉及文件**：新增工具 + 注册 + `operators.json`。
- **收益**：复杂逻辑不再依赖外部，对齐 VM K。

### P1-5 缺陷检测专用算子（SurfaceDefect / ContourCompare）
- **问题**：表面缺陷/轮廓比对无专用算子，需手动组合。
- **动作**：新增 `SurfaceDefectTool`（模板差分+形态约束+灵敏度）、`ContourCompareTool`（实测轮廓 vs 标准轮廓偏差判定）；底层复用 `ImageArithmetic` + `RegionGrowing` + `EdgesSubPix`。
- **涉及文件**：新增工具 + 注册 + `operators.json`。
- **收益**：缺陷检测开箱即用，对齐 VM H。

---

## P2 — 性能与精度提升

### P2-1 DL 推理真实化 + GPU 默认路径
- **问题**：`InferencePanel` 推理仍为 `QRandomGenerator` 模拟；ONNXRuntime 默认 OFF。
- **动作**：完成 `ENABLE_ONNX_RUNTIME` 真实推理接入（接口层已就绪）；默认启用 ONNXRuntime CPU 执行提供，GPU（CUDA）可选；`InferenceEngine` 增加后端自动探测（CUDA→OpenVINO→CPU）。
- **涉及文件**：`src/AI/InferenceEngine.cpp`、`TrainingInferenceView.cpp`、`CMakeLists.txt`
- **收益**：AI 检测从演示变生产可用。

### P2-2 匹配尺度/亚像素增强
- **问题**：`FindShapeModel` 仅旋转无尺度；匹配为像素级。
- **动作**：增加尺度金字塔搜索（各向异性）；匹配分数峰值处做亚像素插值（抛物拟合）。
- **涉及文件**：`src/Vision/FindShapeModelTool.cpp`、`FindNccModelTool.cpp`
- **收益**：缩放工件匹配率↑、定位精度达亚像素。

### P2-3 工具链并行与失败快速返回
- **问题**：`execute()` 同步顺序、无并行；主流程未接 `executeAsync`。
- **动作**：对无数据依赖的同源多 ROI 分支启用 `QtConcurrent` 并行；`stop()` 原子标志已在，补充超时预算；长耗时 DL 算子强制独立线程避免阻塞 UI。
- **涉及文件**：`src/Vision/ToolChainExecutor.cpp`
- **收益**：多目标/大图场景节拍达标。

### P2-4 OpenCV 计算优化
- **问题**：部分算子未做 ROI 限定与缓存。
- **动作**：算子内部按 ROI 裁剪后再算；`ImagePreprocessor` 已用 `scanLine`（P1-C11 提速 5-10x），推广至 `GeometryMeasure`/`ContourAnalyze`；`UMat`/GPU（`cv::cuda`）可选加速滤波与二值化。
- **涉及文件**：相关 `*Tool.cpp`
- **收益**：单图处理时延下降，满足产线节拍。

### P2-5 通信算子化与串口补全
- **问题**：`SerialCommunicator` 为存根；通信是独立模块未纳入算子链。
- **动作**：实现 `SerialCommunicator`（接入 `QSerialPort`）；新增 `CommTool` 算子（TCP/串口发送结果，置于链末端），对齐 VM K.5；IO 触发接入 `LightControl`（P0-6）。
- **涉及文件**：`src/Communication/SerialCommunicator.cpp`、`include/Vision/CommTool.h/.cpp`
- **收益**：结果自动上送 PLC/MES，闭环产线联动。

---

## 落地路线建议（里程碑映射）

| 阶段 | 内容 | 对应优先级 |
|---|---|---|
| 立即（本周） | P0-1 幻影算子止血 + P0-2/3 位置修正&单位换算骨架 | P0 |
| 1 个迭代 | P0-4/5/6 卡尺/相机标定/匹配识别补全；P1-1/2 失败语义&一致性校验 | P0+P1 |
| 2 个迭代 | P1-3/4/5 类型端口/流程控制/缺陷算子；P2-1 DL 真实化 | P1+P2 |
| 持续优化 | P2-2~5 尺度亚像素/并行/OpenCV优化/通信算子化 | P2 |

---

## 关键文件索引（改进落点）

| 文件 | 作用 |
|---|---|
| `config/operators.json` | 算子元数据（删除幻影/补新算子/修正分类） |
| `src/Vision/ToolFactory.cpp` | 算子注册（新增注册项 + createTool 空指针防护） |
| `include/Core/VisionTool.h` | `ToolResult` 增加 typed ports |
| `src/Vision/ToolChainExecutor.cpp` | 失败语义 + 并行 |
| `src/AI/InferenceEngine.cpp` | 后端自动探测 + GPU |
| `src/Communication/SerialCommunicator.cpp` | 串口存根补全 |
| `src/UI/OperatorDescriptors.cpp` | 元数据—实现一致性校验 |

---

*报告二完。详细差距见《算子库对比分析报告》。*
